# Technical Risk Register

**Last updated:** 2026-06-05
**Governing ADR:** ADR-044 (Technical Risk Register)
**Entry count:** 156 concerns (97 resolved) + 24 disagreements

---

## Format

| Field | Description |
|-------|-------------|
| **ID** | `C-xx` for concerns, `D-xx` for disagreements |
| **Tier** | 1 (critical) — 4 (minor) |
| **Description** | What the risk is |
| **Trigger** | When this risk becomes actionable |
| **Source** | Where this risk was identified |
| **Status** | Open / Mitigated / Accepted |

### Tier Definitions

| Tier | Severity | Response |
|------|----------|----------|
| 1 | Critical — correctness or data integrity at risk | Must be addressed before next release |
| 2 | High — architectural degradation or silent failure | Should be addressed within current development cycle |
| 3 | Medium — maintainability or operational risk | Track and address opportunistically |
| 4 | Low — minor or cosmetic concern | Document and defer |

---

## Open Concerns

| ID | Tier | Description | Trigger | Source | Status |
|----|------|-------------|---------|--------|--------|
| C-01 | 3 | **God class: ForecastingModelManager (substantially mitigated, DOWNGRADED from Tier 2).** All 6 ADR-045 extractions complete: E1 (PredictionIOManager), E2 (EvaluationStage), E3 (ReportingStage), E4 (ForecastingStage), E5 (TrainingStage), E6 (ModelPathManager relocation to `data/model_path.py`). `model.py` reduced from 3049 to ~1960 LOC. Root Cause #1 (inverted dependencies) resolved — lower layers now import from `data/` not `managers/`. Remaining: abstract method context parameters; WandB lifecycle template; Pipeline composition container. Clean Architecture audit (2026-04-08) confirmed remaining ISP concern — stages can reach back through mutable references passed at construction (C-45). | When a new stage extraction (E7+) is attempted, verify it follows the ADR-045 frozen context pattern — reach-back through mutable references would re-cement coupling | repo-assimilation + expert-code-review | Open |
| C-03 | 3 | **Ensemble data coupling — first model assumed authoritative (DOWNGRADED from Tier 2).** `evaluation/stage.py:135` hardcodes `ModelPathManager(context.configs["models"][0]).data_raw` to load actuals. `validate_ensemble_raw_data_alignment()` added (2026-04-04) to detect inconsistent raw data across models via file size comparison. The hardcoded `[0]` access remains but misalignment is now detectable. | Ensemble models have genuinely different querysets (different file sizes) but validation not called before evaluation | C-03 investigation | Open |
| C-05 | 3 | **Class-level mutable state in ModelPathManager (DOWNGRADED from Tier 1).** `data/model_path.py:77` declares `_root = None` at class level, set once via lazy init, never reset. Theoretical risk only — every production run operates from a single project root per process. No observed or reproducible scenario. | Hypothetical multi-project tool or test that creates instances from different roots without process isolation | C-05 investigation | Accepted |
| C-06 | 3 | **Dynamic script loading via importlib.** `model.py:258` uses `importlib.util.spec_from_file_location` + `spec.loader.exec_module`. Validation limited to file existence and `hasattr`. Inherent to the plugin architecture — no safe alternative without removing the downstream model extensibility model. | Downstream model has malformed scripts; namespace collision from parallel model loading | C-06 investigation | Accepted |
| C-07 | 3 | **Subprocess execution for ensemble sub-models.** Ensemble training/evaluation runs model scripts as shell subprocesses. Error propagation depends on exit codes and stderr parsing. Indefinite-hang risk mitigated by 7200s timeout (2026-04-04). Stderr parsing is the standard subprocess pattern. **Test gap partially closed (2026-05-20):** `TestSubprocessTimeout::test_timeout_expired_raises_pipeline_exception` and `test_subprocess_failure_raises_pipeline_exception` now verify that `TimeoutExpired` and subprocess failure both raise `PipelineException`. The 7200s timeout constant itself is not tested (would require a real long-running process). Related: C-15 (no timeouts on external operations). | Sub-model script fails silently, returns non-zero without clear error message, or hangs indefinitely | repo-assimilation + test-review (2026-05-20) | Accepted |
| C-08 | 4 | **PipelineConfig singleton read at import time.** Reads pyproject.toml at module import. If imported before working directory is correct, version/config will be wrong. In practice, working directory is always correct before import. | Library imported from unexpected working directory (e.g., during testing) | repo-assimilation | Accepted |
| C-10 | 3 | **No test coverage for visualization, mapping, templates, packaging (partially mitigated).** **Mitigated (2026-05-28):** 34 characterization tests for `HistoricalLineGraph` (20) and `PlotDistribution` (14) in `tests/test_modules/test_visualizations.py`. Both classes now in views-reporting (PR 3). **Updated (2026-05-29):** Visualization, mapping, reports, and templates all extracted to views-reporting (ADR-054). Tests in pipeline-core exercise the re-export shims. Remaining untested in pipeline-core: `PackageManager`, `template_config_modelset.py` `generate()` function. Remaining untested in views-reporting: `MappingModule`, template generators. **Updated (2026-06-05):** `template_config_modelset.py` added with zero tests — no round-trip test verifies generated file is valid Python with callable `get_modelset_config()`. | Any modification to MappingModule, template generators, PackageManager, or template_config_modelset.py | repo-assimilation + test-review (2026-05-20) + ADR-054 (2026-05-29) + test-review (2026-06-05) | Open |
| C-11 | 3 | **Appwrite credential assumption (partially mitigated).** Environment variables for Appwrite auth assumed present. Missing credentials cause runtime failures during upload after significant compute investment. Mitigated by `PredictionStoreConfig.from_environment()` (Phase 6 Task 3, 2026-04-07) — credentials validated fail-loud at startup. Remaining: no ADR documents the Appwrite integration decision itself (C-41). | Appwrite credentials not set in environment, discovered only after training completes | repo-assimilation | Open |
| C-15 | 3 | **No timeouts on external operations (partially mitigated).** Ensemble subprocess execution now has a 7200s timeout (2026-04-04). viewser data fetching, Appwrite uploads, and WandB API calls still run without explicit timeouts. | Upstream service (viewser, Appwrite, WandB) becomes slow but not unreachable; pipeline hangs with no error | expert-code-review | Open |
| C-16 | 3 | **No integration tests.** All ~1269 tests are unit tests with sys.modules-level mocks. No test exercises `execute_single_run()` against even synthetic data. The gap between "all tests pass" and "the pipeline works" is unknown. **Test gap (2026-05-20):** Empirical equivalence between EnsembleManager and DataFrameEnsembleManager was proven manually (synthetic_chorus vs synthetic_choir, MSE=4.34444) but no codified parity test exists in the test suite. The parity proof lives only in the postmortem. If either manager drifts, no automated check detects it. Related: C-30 (no cross-repo contract test). | When the first end-to-end integration test infrastructure is created, verify it covers execute_single_run for all three manager types | expert-code-review + test-review (2026-05-20) | Open |
| C-17 | 3 | **Rolling-origin step mapping assumes temporal contiguity.** `_get_evaluation_step_mappings()` assumes month IDs in the test partition are contiguous. `core_data_sniffer.py:125` (`_check_partition_compatibility()`) validates only min/max month_id — no contiguity check exists anywhere in the validation path. VIEWS data is temporally contiguous by design — the edge case (sparse conflict data with gaps) does not occur in practice. Domain angle: temporal contiguity is a Critical Business Rule of VIEWS conflict forecasting, not an edge case. It should be enforced as a domain invariant, not accepted as a data assumption. Addressed by S-02 (PartitionSet validates non-overlap), S-06 (wires into config sniffer), S-07 (consolidates step-mapping). | Data with missing months enters the evaluation pipeline (e.g., sparse conflict data) | expert-code-review + repo-assimilation (2026-04-22) | Accepted |
| C-18 | 3 | **ReconciliationModule test coverage now substantial (DOWNGRADED from Tier 2).** 14 tests total: 6 worker characterization + 4 parallel orchestration + 4 constructor validation. **Constructor gap closed (2026-05-20):** `TestReconciliationModuleConstructorValidation` verifies `TypeError` on `None` and wrong-type arguments for both `c_dataset` and `pg_dataset` (4 tests). Remaining gap (real multiprocessing) would require integration test infrastructure beyond current scope. Related: C-16 (integration tests). | Subtle concurrency bug not caught by mocked executor; device-specific behavior difference | test-review (2026-05-20) | Accepted |
| C-21 | 3 | **Test suite is 83% green, 7% beige — system-level interaction tests nearly absent.** Suite treats components as independent units. No tests for: wrong-order execution, partial target failure (3/5 succeed), ensemble sub-model resolution mismatch, or decision-support metadata completeness. Leveson/STAMP analysis reveals the deepest blind spot. **Test gap (2026-05-20):** ADR-005 RED/BEIGE/GREEN taxonomy markers not adopted in test docstrings or markers. No `@pytest.mark.red`/`beige`/`green` markers, no filtering infrastructure, no enforcement. The taxonomy exists as policy (ADR-005) but is not operationalized — category balance cannot be measured automatically. | When ADR-005 RED/BEIGE/GREEN markers are operationalized, verify at least 10% of tests are RED (system interaction) — current estimate is <3% | test-review (2026-05-20) | Open |
| C-26 | 2 | **EvaluationAdapter silently truncates on sparse actuals.** `from_dataframes()` produces a truncated EvaluationFrame when actuals partially cover the prediction window (e.g., 3/12 months). No warning logged unless overlap is exactly zero. Metrics computed on truncated data appear normal. Domain angle: truncated evaluation on sparse conflict actuals silently misrepresents model performance — metric validity is a domain-level concern, not just a warning gap. See S-07 (resolve_base_origin) for step-mapping authority that bounds the truncation window. **Test gap (2026-05-20):** No test covers the partial-overlap truncation path — only the zero-overlap case has a test stub (from falsification). The silent truncation is the more dangerous case (produces plausible-looking wrong numbers). Related: C-89 (empty intersection). Tier recalibrated from 3 to 2 during review-rr (2026-06-02). | Data sparsity in actuals (missing months); evaluation metrics computed on partial data without any indication of incompleteness | falsification-audit + test-review (2026-05-20) | Open |
| C-29 | 3 | **`legacy_compatibility=True` is a documented but hardcoded behavioral decision.** Both `evaluator.evaluate()` calls in `evaluation/stage.py` pass `legacy_compatibility=True`, preserving step-wise truncation to shortest sequence. Inline comments added (2026-04-04) explaining rationale and linking to C-29. Decision to flip to `False` deferred pending numeric equivalence verification. Domain angle: the step-wise truncation semantics encode a specific conflict evaluation methodology — flipping this flag is a domain policy decision, not a tech-debt cleanup. S-07 provides the hook for a future story to make this configurable. | Developer flips to `False` without running verification; or flag is never flipped, silently preserving legacy truncation indefinitely | post-migration-audit | Open |
| C-30 | 3 | **No cross-repo contract test pattern.** `test_evaluation_integration.py` catches import breakage (added reactively after 2026-04-03 incident) but does not test signature stability, return type structure, or behavioral changes. No CI matrix, no minimum-version testing. | views-evaluation changes a method signature, return type, or default behavior; pipeline-core tests pass but integration fails | post-migration-audit | Open |
| C-31 | 3 | **Dual evaluation paths are now functionally identical.** PF path and DF path (both in `evaluation/stage.py:94-106`) both end with `evaluator.evaluate(ef=ef, legacy_compatibility=True)`. Only differ in EvaluationFrame construction. Parity audits removed (post-mortem 2026-03-03). No tracked retirement timeline for DF path. Two code paths without active parity verification increase maintenance burden and divergence risk. | Maintenance change to one path but not the other; or refactoring attempt that must update both paths in lockstep | post-migration-audit | Open |
| C-34 | 3 | **`_execute_model_evaluation()` is the most complex unextracted method (226 LOC, partially mitigated).** Contains PF/DF dual-path dispatch, streaming origin sink closure with 5 nonlocal captures, staging directory management, mmap reload, and threadpool validation. 10 characterization tests added (2026-04-06) covering DF path, PF streaming, skip-metrics, no-metrics, WandB lifecycle, and sequence count validation. Origin sink closure remains an ad-hoc Visitor with mutable state. Tier recalibrated from 2 to 3 during review-rr (2026-06-02). | E3-E5 extraction modifying adjacent code in `_execute_model_evaluation()` without checking characterization tests; or origin sink closure mutation bugs during refactoring | expert-code-review (2026-04-06) | Open |
| C-35 | 3 | **God class: AppWriteFileModule (1,724 LOC at time of audit, 22 methods).** `modules/appwrite/file.py`. 7 responsibility areas. Upload methods alone are 700 LOC (`upload_file_with_metadata` is 449 LOC). Auth/cache/metadata already partially extracted to collaborators. Decomposition candidates: (1) UploadOrchestrator — 4 upload methods, 700 LOC; (2) FileHashingService — hash + dedup, 119 LOC; (3) BucketManager — 3 bucket methods, 170 LOC; (4) UserInfoAccessor — 2 auth-query methods, 93 LOC. Live metrics: `test_falsification_no_god_classes.py::test_P2`. ISP violation confirmed (C-44) — `DatastoreModule` uses 1 of 16 public methods. No narrowing protocol. | When the next storage operation is added to AppWriteFileModule, or when upload workflow changes require touching >3 of the 7 responsibility areas simultaneously | falsification-audit (2026-04-07) | Open |
| C-36 | 3 | **God class: _ViewsDataset (partially mitigated by ADR-054 extraction).** `data/handlers.py`. Originally 1,621 LOC / 46 methods / 22 public. ~1,342 LOC of misplaced methods (statistics, HDI, MAP, reconciliation export, visualization helpers) extracted to views-reporting in PR 8. Residual: ~953 LOC, 7 classes (full inheritance hierarchy), core responsibilities: validation, tensor conversion, dataframe management. Phase 2 decomposition remains: (1) DatasetValidator, (2) TensorConverter. Live metrics: `test_falsification_no_god_classes.py::test_P3`. | When _ViewsDataset Phase 2 decomposition begins (DatasetValidator/TensorConverter extraction), or when a new spatial resolution requires a fourth dataset subclass beyond PGM/CM/CY | falsification-audit (2026-04-07) + ADR-054 extraction (2026-05-29) | Open |
| C-38 | 3 | **No domain entity layer — Critical Business Rules dissolved into mechanism code.** Architecture screams "PIPELINE" (`managers/`, `modules/`, `data/`) not "CONFLICT FORECASTING SYSTEM." Spatial level is a bare string (`"cm"`/`"pgm"`) in 15+ files. Temporal partitions are untyped dicts. Forecast horizon is hardcoded as `SUPPORTED_TIME_STEPS={36}`. Rolling-origin step-mapping formula duplicated in `model.py:1715` AND `stage.py:251`. Reconciliation invariants (sum constraint, zero-preservation, non-negativity) implicit in tensor ops. Per Clean Architecture Ch.20-22: Entities should be innermost circle, unsullied by mechanism. Stories: S-01 (SpatialLevel), S-02 (TemporalPartition), S-03 (ForecastHorizon), S-04 (ReconciliationInvariants). Clean Architecture audit (2026-04-08) confirmed: screaming architecture test failed — top-level directories scream "pipeline mechanism" not "conflict forecasting system." Boundary enforcement absent (C-46). Deferred imports indicate circular edges (C-47). Tier recalibrated from 2 to 3 during review-rr (2026-06-02). | When S-01 (SpatialLevel) implementation begins — verify all new domain concepts use typed objects; or when a third spatial level is added as another bare string | expert-code-review (2026-04-07) | Open |
| C-39 | 3 | **Rolling-origin step-mapping formula duplicated in `model.py:1715-1718` and `stage.py:251-253`.** Both compute identical `{base_origin + i + s: s}` dicts. Both duplicate the base_origin resolution logic (forecasting vs calibration/validation branching). Fix to one location requires finding and patching the other. Story S-07 consolidates via `ForecastHorizon.build_step_mappings()`. | Any modification to rolling-origin evaluation semantics | expert-code-review (2026-04-07) | Open |
| C-40 | 2 | **At >64 samples, prediction persistence + evaluation paths exceed typical workstation RAM (~16 GB).** `to_prediction_df()` causes 33x memory explosion (measured: 4,766 MB peak for 179 MB PredictionFrame). `from_prediction_frames()` loads all sequences simultaneously (measured: 3,138 MB). Arrow zero-copy write (`to_arrow_table()`) mitigates save-path peak but evaluation-path scaling remains. At 252 samples pgm scale: ~64 GB projected peak. `NpzSaver` (Phase 6 Task 2) bypasses conversion entirely for internal storage; `LocalParquetSaver` uses Arrow zero-copy for delivery format. Future: zarr for chunked cloud-native access when downstream consumers migrate. | Model configured with >64 samples at pgm level; or global grid at any sample count | expert-code-review (2026-04-07) | Open |
| C-42 | 3 | **Missing CICs for 5 key data-flow classes (partially resolved).** Originally no CIC for: (1) `ViewsDataLoader`; (2) `AppWriteFileModule`; (3) `WandBModule`; (4) `EvaluationStage`; (5) `ForecastingStage`. **Updated (2026-04-22):** `ViewsDataLoader` CIC written and updated to document dual-source dispatch (PR5). `EvaluationStage` CIC already existed. **Updated (2026-05-06):** `ForecastingStage` CIC existed since Phase 6 extraction; updated §3 with per-target filename uniqueness guarantee (multi-target bugfix). Remaining: `AppWriteFileModule`, `WandBModule`. | When the next PR touches AppWriteFileModule's or WandBModule's public interface — CIC must exist before the change lands | Clean Architecture audit (2026-04-08) | Open |
| C-44 | 3 | **ISP violation: `AppWriteFileModule` fat interface.** `AppWriteFileModule` exposes 16 public methods. `DatastoreModule` uses only `upload_file_with_metadata()`. No narrowing protocol exists — clients depend on the full 1,724-LOC class. Per ISP: "Don't depend on things you don't need." Changes to unused methods (bucket management, user info, cache) force redeployment of all dependents. Related: C-35 (god class). | Change to unused `AppWriteFileModule` method causes import-time side effects or test breakage in unrelated code | Clean Architecture audit (2026-04-08) | Open |
| C-45 | 3 | **ISP violation: `ForecastingModelManager` exposes full surface to stages.** Stages receive frozen context objects (good), but are instantiated inside `__init__` with `self._io`, `self._wandb_module` passed by reference. Stages can reach back through mutable references to access ~30+ methods they don't need. The abstraction boundary is partially clean but leaky. Related: C-01. | Stage code grows to depend on manager internals accessible through injected references | Clean Architecture audit (2026-04-08) | Open |
| C-50 | 3 | **`validate_ensemble_model` (L2/L5 validator) instantiates L6 orchestrators.** `modules/validation/ensemble/check.py:238-244` imports `ModelManager` from `managers.model` and `EnsembleManager`/`EnsemblePathManager` from `managers.ensemble`, then constructs them directly to run partition-alignment validation. This is a real ADR-002 Rule 1 violation (Layer 2/5 reaching up to Layer 6). Currently contained by a *granular* exemption in `tests/test_boundary_enforcement.py` — only those three names are whitelisted; any additional manager import still fails CI. Correct fix is moving the function into `managers/ensemble/` with a deprecation shim, which is a public API change and belongs in a separate PR. | New manager import added to `check.py`; or refactor of ensemble validation flow that surfaces additional L6 dependencies | C-46 mitigation (2026-04-11) | Open |
| C-47 | 3 | **Deferred imports indicate unresolved circular dependencies.** 6+ deferred imports in stage classes: `forecasting/stage.py` defers `PredictionFrameConverter`, `CorePredictionSniffer`, `handle_single_log_creation`; `evaluation/stage.py` defers `NativeEvaluator`, `EvaluationAdapter`; `training/stage.py` defers `wandb`, `handle_single_log_creation`; `prediction_store.py` defers `AppwriteConfig`. Pragmatic workaround but indicates the dependency graph has cycles that should be resolved via protocols or layer relocation. | New import added at module level triggers circular import error, requiring another deferred import as band-aid | Dependency graph analysis (2026-04-08) | Open |
| C-48 | 3 | **Concrete dependencies where abstractions needed.** 4 key collaborators used concretely with no protocol/interface: `PredictionIOManager`, `DatastoreModule`, `WandBModule`, `ViewsDataLoader`. Martin's DIP: "Don't refer to volatile concrete classes." All are actively developed (volatile). `ModelPathProtocol` in `types.py` proves the pattern works and should be extended. **Graph evidence (2026-04-22, updated 2026-06-02):** April run: `ModelPathManager` had the highest betweenness centrality (0.132), bridging 6 communities. June run (5,489-node graph, 13,016 edges, 279 communities): `PredictionFrame` is the #1 degree-centrality hub (456 edges), surpassing `ForecastingModelManager` (436) and `ModelPathManager` (332). PredictionFrame's dominance reflects the ongoing PF adoption — it now connects more code paths than the former god class. ModelPathManager remains the highest-priority protocol extraction candidate (most cross-cutting bridge node), but PredictionFrame's coupling surface is growing fastest and should be monitored. | When WandB → MLflow migration is evaluated, or when the next stage or manager type needs PredictionIOManager or ViewsDataLoader — extract Protocol before adding the new consumer | Clean Architecture audit (2026-04-08) + graphify (2026-04-22) | Open |
| C-55 | 2 | **EnsembleManager `execute_single_run()` duplicates parent and drops safety guarantees.** `ensemble.py:120-163` overrides `ForecastingModelManager.execute_single_run()` (model.py:910-983), duplicating WandB login, config update, and project setup — but omits `CoreConfigSniffer.sniff_all()`, `_assert_partition_config_accessible()`, and `_execute_data_fetching()`. The ensemble validation path (`validate_ensemble_model`, `check.py`) checks temporal freshness, deployment status, and partition alignment — but skips `_check_targets_and_metrics()`, `_check_prediction_format()`, `_check_evaluation_mode()`, `_check_evaluation_contract()`, `_check_currently_supported_values()`, and `_check_reconciliation_config()`. Parent improvements to the execution flow (new precondition checks, new stages) will not propagate to the ensemble path. **Falsification evidence (2026-05-20):** C-78 fix added `_check_reconciliation_config()` to CoreConfigSniffer — but since EnsembleManager never calls CoreConfigSniffer, the fix was dead code on the primary ensemble path. All 5 production ensembles (white_mustang, skinny_love, cruel_summer, pink_ponyclub, rude_boy) run via EnsembleManager. DataFrameEnsembleManager IS protected (C-55 fix wired CoreConfigSniffer at line 262). 8-expert review (2026-05-20) identifies three fix options: (A) add CoreConfigSniffer call to EnsembleManager, (B) Template Method in parent, (C) convert EnsembleManager to composition — see D-17. **Bridge fix (2026-05-20):** Option A applied — `CoreConfigSniffer(self.configs, self._partition_dict).sniff_all(args.run_type)` added to `EnsembleManager.execute_single_run()` before WandB login (ensemble.py:137). Parameterized regression test (`test_falsification_merge_readiness.py`) asserts CoreConfigSniffer runs on all three manager types (EnsembleManager, DataFrameEnsembleManager, ForecastingModelManager). This is a bridge fix — permanent resolution is retiring EnsembleManager in favour of DataFrameEnsembleManager (D-13 trajectory). The structural LSP violation (C-65) and fragile-base-class problem remain: new preconditions added to `ForecastingModelManager.execute_single_run()` still won't propagate to the ensemble override. Related: C-50 (layer violation in `check.py`), C-65 (LSP violation — root cause of the override), C-78 (now active on this path), D-17 (resolved as Option A + C roadmap). | New precondition added to parent `ForecastingModelManager.execute_single_run()` does not propagate to the ensemble override; permanent fix is retiring EnsembleManager → DataFrameEnsembleManager (D-13) | repo-assimilation (2026-04-22) + expert-review (2026-05-18) + falsification (2026-05-20) | Open |
| C-57 | 3 | **PipelineConfig property triggers uncached GitHub API call.** `configs/pipeline.py:38-39`: `views_pipeline_core_version_range` property lazily imports `PackageManager` and calls `get_latest_release_version_from_github('views-pipeline-core')`. No caching, no timeout, no fallback. In CI or air-gapped environments, property access causes hangs or failures. The call also crosses an architectural boundary (Layer 0 configs/ → Layer 5 managers/package), already exempted in `test_boundary_enforcement.py:152-158`. Related: C-08 (PipelineConfig singleton), C-15 (no timeouts on external operations). | `views_pipeline_core_version_range` accessed in air-gapped CI, GitHub API rate-limited environment, or during template generation with no network | repo-assimilation (2026-04-22) | Open |
| C-58 | 3 | **`files/utils.py` has partial test coverage for a critical-path utility hub (partially mitigated).** `tests/test_utils/test_files.py` covers 4 of 10 functions (`read_log_file`, `create_data_fetch_log_file`, `create_specific_log_file`, `save_dataframe`). **Mitigated (2026-04-22):** `handle_single_log_creation` now tested via `test_get_data_dispatch.py` (missing log + existing log) and `test_dataloader_characterization.py` (PR4). **Further mitigated (2026-05-06):** `generate_output_file_name` now tested (5 tests in `TestGenerateOutputFileNameTarget`) covering backward compat and target_identifier. Remaining untested critical-path functions: `read_dataframe` (6 call sites), `create_log_file`, and 3 filename generators (`generate_model_file_name`, `generate_evaluation_file_name`, `generate_evaluation_report_name`). Related: C-10 (no test coverage for visualization, mapping, templates, packaging). | Modification to `read_dataframe()` or log file format change affecting `read_log_file()`; any refactor to shared I/O utilities without regression tests for untested functions | repo-assimilation (2026-04-22) | Open |
| C-59 | 2 | **Cache filename convention hardcoded across 5 locations in 3 engine repos — shotgun surgery on any change.** `views-pipeline-core` constructs cache filenames as `{partition}_{source}_df{ext}` (`dataloaders.py:1425-1428`), but engine repos independently hardcode the `_viewser_df` substring: `views_dataset_darts.py:46` in views-r2darts2, `data_fetcher.py:44` in views-hydranet, and `bright_starship/main.py:30` in views-models. `model_path.py:590-591` handles both patterns for discovery, but no shared constant or function exists for filename construction. Adding a new data source or changing the naming convention requires finding and patching every hardcoded location — the exact failure mode that caused the current `FileNotFoundError` when datafactory models expected `_datafactory_df.parquet` but engines looked for `_viewser_df.parquet`. 4/8 experts flagged this as the primary coupling defect. Related: C-14 (resolved — pipeline-core side), C-30 (no cross-repo contract test). | When a new data source is added to views-pipeline-core, or the cache filename convention changes, engine repos fail with `FileNotFoundError` because they independently hardcode `_viewser_df` | expert-review (2026-04-27) | Open |
| C-60 | 2 | **Phase 1 workaround in `bright_starship/main.py` still active after underlying fix (PR #69) landed.** `_ensure_data()` (lines 24-39) fetches datafactory data and saves it as `{run_type}_viewser_df.parquet` (line 30) — deliberately disguising the data source to survive the hardcoded `_viewser_df` filename in views-hydranet. `args.saved = True` (line 51) bypasses `_detect_data_source()`, `_fetch_data_from_datafactory()`, and all pipeline validation including `CoreDataSniffer`. The workaround was designed for C-51 (now resolved). Its continued presence: (a) creates false filesystem provenance (datafactory data under viewser filename), (b) masks whether PR #69 actually works for bright_starship, (c) will cause confusing debugging when bright_starship's config changes. Related: C-51 (resolved), C-53 (resolved), C-59 (filename coupling), D-09 (workaround safety). | When bright_starship's data source, features, or loa is modified, the workaround's stale cache and bypassed validation will cause silent wrong-data training or confusing `FileNotFoundError` | expert-review (2026-04-27) | Open |
| C-61 | 2 | **No cache provenance verification when `use_saved=True` — stale data loaded silently if index matches.** `get_data()` (`dataloaders.py:1431-1439`) loads cached parquet when `use_saved=True` without verifying the cache was produced by the current config. `CoreDataSniffer` validates MultiIndex structure (level, index names, partition range) but NOT column content, feature set, or descriptor hash. If a model changes its queryset features or region while a cached parquet from the old config exists with the same level, the sniffer passes and training proceeds on stale features. C-14's resolution (source-encoded filenames) handles source-switching staleness; this concern is the explicitly deferred "broader data-versioning concern (queryset hashes, manifests)" from C-14. **Additional dimension (2026-06-03, falsification):** `use_saved=True` with a *missing* cache file silently falls back to a network fetch (`dataloaders.py:1504-1506`). The `saved` flag semantically means "prefer cache, fall back to fetch" — not "require cached data." This means the three-site `saved` invariant (C-146) provides a weaker guarantee than its documentation implies: non-training runs may still trigger fresh data fetches when cache files are absent. Related: C-14 (resolved — source dimension only), C-60 (workaround creates false-provenance cache), C-146 (three-site invariant — relies on `saved` semantics). | When a developer changes a model's queryset features, region, or feature rename mapping, then runs with `--saved` (or a workaround sets `args.saved=True`), stale cache with correct index but wrong features is loaded without error; OR when cache is absent, `use_saved=True` silently fetches fresh data instead of failing | expert-review (2026-04-27) + falsify (2026-06-03) | Open |
| C-62 | 3 | **Output format string literals duplicated between views-pipeline-core and views-datafactory with no shared constant.** `_LOA_TO_OUTPUT_FORMAT` (`dataloaders.py:30-33`) maps `{"priogrid_month": "dataframe", "country_month": "country_month"}`. `_VALID_FORMATS` (`views-datafactory/src/datafactory_query/dataset.py:35`) defines `("feature_frame", "dataframe", "country_month")`. These are independent string literals with no import-time validation. If views-datafactory renames a format (e.g., `"country_month"` ��� `"country_level"`), views-pipeline-core's `load_dataset()` call fails at runtime with no compile-time signal. Related: C-30 (no cross-repo contract test). | When views-datafactory renames or adds an `output_format` string, views-pipeline-core's `_LOA_TO_OUTPUT_FORMAT` must be updated in lockstep — no CI or import-time validation catches the mismatch | expert-review (2026-04-27) | Open |
| C-65 | 2 | **LSP violation: EnsembleManager inherits ForecastingModelManager but violates its abstract contract.** `EnsembleManager` (ensemble.py:82) extends `ForecastingModelManager` but cannot fulfill the parent's abstract method promises. `_train_model_artifact()` (ensemble.py:369-382) accepts `model_name: str` and delegates to subprocess — the parent contract (model.py:588-624) promises no-argument `_train_model_artifact() -> any` that loads data via `self._data_loader` and returns a trained model. `_evaluate_model_artifact()` (ensemble.py:384-421) takes `model_name: str` and loads DataFrames from disk — parent expects `(eval_type, artifact_name)` and returns predictions from an in-memory model. `_forecast_model_artifact()` (ensemble.py:423-446) takes `model_name: str` — parent expects `artifact_name: str` with different semantics. No `_evaluate_sweep()` override exists. An ensemble is-not-a forecasting model; the inheritance exists only to reuse infrastructure (`_wandb_module`, `_io`, `_evaluation_stage`, `configs`, `_save_predictions()`). The DIP is also violated: shared infrastructure is accessible only through the inheritance chain. Related: C-55 (consequence — override drops safety), D-03 (architectural disagreement), C-01 (god class enables the coupling). | When ForecastingModelManager adds a new abstract method, or existing abstract method signatures change, or a caller invokes ensemble through the parent interface expecting the parent contract | expert-review (2026-05-18) | Open |
| C-66 | 2 | **OOM on DataFrame ensemble aggregation with sample-based models at pgm scale.** `_get_aggregated_df()` (ensemble.py:753-822) delegates to `AggregationManager` (aggregator.py) which materializes all predictions as list-in-cell DataFrames in Polars. For a 5-model Africa+ME ensemble with 64 posterior samples: ~16.5 GB peak (5 × 64 × 180×180 grid × 8 bytes × overhead). For global coverage (heavy_freighter, 360×720 grid): ~82 GB — physically impossible on any current workstation. No memory budget check, no circuit breaker, no progressive loading. The process dies via OOM killer with no WandB alert, no partial results, no recovery. This is the root blocker preventing the existing ensemble path from handling PredictionFrame models with sample estimates. The new PredictionFrame ensemble path MUST aggregate via dense NumPy arrays, never materializing list-in-cell format. Related: C-40 (single-model PF memory — different mechanism), C-65 (LSP violation — the reason the new path can't just extend EnsembleManager). | When any ensemble is configured with constituent models producing sample-based predictions (sample_count > 1) at pgm level and uses the existing `_get_aggregated_df()` → `AggregationManager` path | expert-review (2026-05-18) | Open |
| C-67 | 3 | **No schema pre-validation before ensemble aggregation.** `_get_aggregated_df()` (ensemble.py:753-822) assumes all constituent DataFrames share identical MultiIndex structure and column sets. `target_cols` is constructed from `self.configs.get("targets")` (the ensemble config), but no check validates that each constituent model's DataFrame actually contains those columns with matching dtypes. `validate_ensemble_raw_data_alignment()` (check.py:163-206) compares file sizes as a proxy for queryset consistency but does not inspect schemas. If a constituent model is updated to change its index structure, add derived columns, or use different target naming, `AggregationManager.add_model()` will either raise a cryptic Polars error or silently produce misaligned aggregation. Related: C-03 (first model assumed authoritative — different concern, same file area). | When a constituent model is independently updated to change its output schema (new targets, different index levels, renamed columns) and the ensemble is re-evaluated without re-running validation | expert-review (2026-05-18) | Open |
| C-93 | 4 | **Double-underscore `__reconcile_pg_with_c` in `ensemble.py` prevents extraction.** `ensemble.py:690` uses `__reconcile_pg_with_c` (name-mangled to `_EnsembleManager__reconcile_pg_with_c`) while `dataframe_ensemble.py:868` uses `_reconcile_pg_with_c` (single underscore). Name mangling makes the method inaccessible from outside the class, preventing extraction into a shared mixin or utility. Test code at `test_ensemble_manager.py:967` also uses the mangled name (`_EnsembleManager__activate_reconciliation`). Deferred: EnsembleManager is slated for retirement (D-13); fixing the naming requires updating all internal references. Related: C-65 (LSP violation), D-13 (retire EnsembleManager). | When reconciliation logic is extracted to a shared base or mixin as part of D-13, the double-underscore method is inaccessible from outside the class | tech-debt-cleanup (2026-05-20) | Open |
| C-101 | 3 | **6 Dependabot vulnerability alerts on default branch (2 high, 3 moderate, 1 low).** GitHub reports 6 dependency vulnerabilities. **pytest (CVE-2025-71176, MEDIUM):** Fixed — bumped to `^9.0.3`. **geopandas (CVE-2025-69662, HIGH):** Cannot fix — geopandas >=1.1.2 requires pandas >=2.0 but 8 views-* packages pin `pandas <2.0`. Vulnerable function `to_postgis()` is never called in this codebase. **pywin32 (CVE-2021-32559, HIGH):** Not applicable — Windows-only, not installed on Linux. **python-dotenv (CVE-2026-28684, MEDIUM):** Cannot fix — `ingester3=2.1.1` pins `python-dotenv >=0.18,<0.19`. **diskcache (CVE-2025-69872, MEDIUM):** No patched version exists. **paramiko (CVE-2026-44405, LOW):** No patched version exists. Remaining 5 alerts require upstream dependency releases. | A security audit reviews the dependency tree — the geopandas alert is the only HIGH-severity item with a real (but unreachable) vulnerability in this codebase | git push output (2026-05-23) + remediation (2026-05-27) | Open |
| C-104 | 4 | **Star imports (`from .X import *`) in 4 `__init__.py` files prevent static analysis of public API surface (partially mitigated).** `exceptions/__init__.py`, `files/__init__.py`, `modules/validation/ensemble/__init__.py`, and `modules/wandb/__init__.py` re-export via `import *`. **Updated (2026-05-29):** 6 extracted module `__init__.py` files (statistics, visualizations, mapping, reports, reconciliation, transformations) were replaced by explicit named re-export shims during ADR-054 extraction. The 4 original star-import files remain unchanged. | A developer adds a new name to `exceptions.py`, `files/utils.py`, `ensemble/check.py`, or `wandb/utils.py` that collides with a name at a consumer's import site — silent shadowing with no lint warning | lint-housecleaning (2026-05-24) | Open |
| C-105 | 3 | **[views-reporting]** **Eval report sample graphs scale-blind — `_add_prediction_sample_graphs()` loads ALL entities and creates one Plotly trace per entity.** **Updated (2026-05-29):** Code now lives in `views-reporting` (`templates/reports/evaluation.py` and `visualizations/historical.py`) after ADR-054 extraction. The scale problem is unblocked — Sprint 4 can now address it in views-reporting with proper rendering infrastructure. Underlying issue unchanged: PGM scale (172k grid cells) produces 172k traces and multi-GB HTML. Designed for CM (~50 entities). Related: C-10, C-36, C-40, C-106. Tier recalibrated from 2 to 3 during review-rr (2026-06-02). | Management requests feature parity between DF and PF evaluation reports — sample prediction graphs must be reimplemented with entity sampling and numpy-backed data source in views-reporting | Track B retirement investigation (2026-05-26) + ADR-054 (2026-05-29) | Open |
| C-106 | 3 | **[views-reporting]** **`PGMDataset` used as general-purpose visualization data source has no scale guard.** `data/handlers.py` `_ViewsDataset._init_dataframe()` eagerly materializes ALL rows. **Updated (2026-05-29):** The visualization consumers (`HistoricalLineGraph`, `EvaluationReportTemplate`) now live in views-reporting (ADR-054). The `_ViewsDataset` class and its `_preprocess_dataframe()` bottleneck remain in pipeline-core `data/handlers.py`. Scale guard should be added in views-reporting's consumption path, not in the core data container. Related: C-36, C-40, C-105. | A developer passes PGM-scale data with samples to `PGMDataset` for visualization or analysis — OOM or multi-minute hang with no diagnostic message | Track B retirement investigation (2026-05-26) + ADR-054 (2026-05-29) | Open |
| C-108 | 3 | **`_origin_sink` closure has zero multi-origin test coverage — missing-target warning and `n_sequences` counter untested.** All test stubs call `origin_sink(0, {...})` exactly once. Production calls it 13 times (MAX_SHIFT_COUNT + 1 rolling origins). The closure's `all_targets` accumulation (line 1311-1312), missing-target warning (lines 1314-1320), and `nonlocal n_sequences` counter (line 1352) are never exercised across multiple invocations. A bug in the `all_targets` comparison or `n_sequences` increment would not be caught. The DF path has comparable depth for its save loop. The `n_sequences` counter additionally feeds the WandB alert message (line 1399) and the metrics reload loop (`range(n_sequences)` at line 1425) — its correctness is assumed but never verified against actual origin count. Related: C-16 (no integration tests), C-10 (no viz test coverage). | A developer modifies the closure's target tracking or origin counting logic (e.g., to support variable target sets across origins) — no test verifies multi-origin interactions | falsify (2026-05-26, test coverage parity audit) | Open |
| C-113 | 3 | **[views-reporting]** **VIEWSER network call hidden in `HistoricalLineGraph` rendering path.** **Updated (2026-05-29):** `historical.py` now lives in `views-reporting/visualizations/` after ADR-054 extraction (PR 3). The VIEWSER network call persists at the same relative location. Risk unchanged — location moved from pipeline-core to views-reporting. Related: C-15, C-10, C-105. | VIEWSER is slow or unreachable during eval/forecast report generation — graph section fails with a network error that does not mention VIEWSER | expert-code-review (2026-05-27) + ADR-054 (2026-05-29) | Open |
| C-114 | 3 | **[views-reporting]** **`ForecastReportTemplate` has same PGM graph gap as eval report — `isinstance(_CDataset)` guard silently skips all PGM graphs.** **Updated (2026-05-29):** `forecast.py` now lives in `views-reporting/templates/reports/` after ADR-054 extraction (PR 6). Risk unchanged — location moved from pipeline-core to views-reporting. The `isinstance` guard and PGM graph gap persist. Related: C-105, C-106. | Management requests PGM forecast report parity — developer removes the `isinstance` guard without adding entity sampling, causing 172k-trace explosion | expert-code-review (2026-05-27) + ADR-054 (2026-05-29) | Open |
| C-112 | 2 | **pandas ecosystem lock — 8 views-* packages pin `pandas <2.0`, blocking all modern dependency upgrades.** `viewser` (>=1.4.0,<2.0.0), `views_evaluation` (>=1.5.3,<2.0.0), `views_forecasts` (>=1.2.3,<2.0.0), `views-partitioning` (>=1.3.3,<2.0.0), `views_stepshifter` (>=1.5.3,<2.0.0), `views_storage` (>=1.3.5,<2.0.0), `views-tensor-utilities` (>=1.5.0,<2.0.0), `views-transformation-library` (>=1.2.3,<2.0.0) all pin pandas <2.0 in their published requirements. This blocks: (1) geopandas >=1.1.2 (CVE-2025-69662 fix), (2) any future library requiring pandas 2.0+. **Empirical evidence (2026-05-27):** force-installing pandas 3.0.3 with `--no-deps` resulted in 1385/1386 tests passing in views-pipeline-core — one assertion needed a forward-compatible fix (string dtype representation change). The pins appear overly conservative. Coordinated multi-repo bump required: all 8 packages must release with relaxed pandas pins simultaneously. Related: C-101 (blocked geopandas alert), C-30 (no cross-repo contract test). Tier recalibrated from 3 to 2 during review-rr (2026-06-02). | When any required library (not just geopandas) mandates pandas >=2.0 as minimum, or when the pandas 1.x line reaches EOL and stops receiving security patches — the entire platform is frozen on an unsupported pandas version | Dependabot remediation investigation (2026-05-27) | Open |
| C-133 | 3 | **[views-reporting]** **`report.py:213` `except ImportError` fallback violates ADR-008 — silent degradation without logging.** `ReportModule.add_markdown()` catches `ImportError` for the `markdown` package and falls back to plain text rendering. The module does not import `logging` at all — no `logger.warning()` before degradation. ADR-008 §Degraded Operation requires "Log at WARNING + document scope of degradation." The fallback adds a user-visible message in the HTML output ("Markdown rendering unavailable") but is programmatically invisible — monitoring cannot detect the degradation. While `markdown` is a declared dependency in `pyproject.toml` (so this only fires with broken installations), the governance violation stands: this is the same "silent degradation via `except ImportError`" pattern that makes C-132's shim defect dangerous. Falsification tests: `TestF5_ReportSilentDegradation` (2 tests) in `tests/test_falsification_try_except_critical_infra.py`. Related: C-22 (Closed — ADR-008 violations fixed for PipelineException raises, but not ImportError fallbacks), C-68 (Resolved — reconciliation silent failure), C-132 (same `except ImportError` pattern class). | `markdown` package not installed or broken in a deployment environment — report renders plain text without any log entry; monitoring has no signal that reports are degraded | falsify (2026-05-28, try/except critical infrastructure audit) | Open |
| C-134 | 3 | **Test files import via direct file paths, bypassing `__init__.py` re-export shims (partially mitigated by extraction PRs).** **Updated (2026-05-29):** During PRs 1-8, test file imports were updated to use package paths as each module was extracted. Most direct-file imports are now fixed. Remaining: scan needed to confirm all test files use package-level imports for extracted modules. The systemic pattern (direct file paths in test imports) should be enforced by a lint rule or CI check to prevent recurrence. Related: C-121 (Resolved), C-132 (Resolved). Tier recalibrated from 2 to 3 during review-rr (2026-06-02). | A developer adds a new test importing via direct file path — bypasses `__init__.py` shim, test breaks when underlying file moves | falsify (2026-05-28, PR 1 extraction audit) + ADR-054 (2026-05-29) | Open |
| C-135 | 3 | **Private attribute coupling across package boundary after extraction.** `transformations.py:119-120` accesses `dataset._time_id` and `dataset._entity_id` — private attributes on `_ViewsDataset` (set at `handlers.py:108`). After extraction to views-reporting, this becomes cross-package private attribute access. `_ViewsDataset` has private methods `_get_time_index()` and `_get_entity_index()` but no public accessors. If `_ViewsDataset` renames these attributes (standard refactoring of private internals), views-reporting breaks with `AttributeError` at runtime with no compile-time or lint signal. Falsification tests: `TestF2_PrivateAttributeCoupling` (1 test) in `tests/test_falsification_pr1_extraction.py`. Related: C-36 (god class `_ViewsDataset`). | `_ViewsDataset` renames or removes `_time_id`/`_entity_id` as part of a refactoring — `transformations.py` in views-reporting breaks with `AttributeError` at runtime | falsify (2026-05-28, PR 1 extraction audit) | Open |
| C-140 | 2 | **TEMPORARY transform undo responsibility ambiguous per-model — silent double-undo or missing-undo corrupts predictions.** `model.py:~2326-2336` (PF forecast path) and `model.py:~2351-2361` (DF forecast path) contain `# TEMPORARY: Undo transformations before saving per target.` blocks. Whether these execute depends on the individual model's code path. If a constituent model already undoes its own log-transforms before returning a PredictionFrame, AND pipeline-core's TEMPORARY block also fires, predictions are double-exponentiated (orders of magnitude too large). If neither undoes transforms, predictions are in log-scale presented as natural scale. Both failures are silent — values look like valid predictions (positive numbers) with no error signal. Related: C-66 (OOM — motivates PF adoption), C-40 (PF memory scaling). | Switching a constituent model to `prediction_format: "prediction_frame"` without verifying whether that model or pipeline-core's TEMPORARY block handles the log-transform undo — first production ensemble forecast produces silently corrupted predictions | expert-code-review (2026-06-01, PF ensemble path forward review) | Open |
| C-141 | 3 | **Concat aggregation order in PredictionFrameEnsembleManager depends on config model list order with no explicit sort.** `_aggregate_prediction_frames()` (prediction_frame_ensemble.py:97) calls `np.concatenate([pf.y_pred for pf in frames], axis=1)`. The `frames` list ordering comes from iterating `ctx.models` (a list from config) and collecting subprocess results. For `arithmetic_mean` this doesn't matter (commutative). For `concat`, column position encodes model provenance — if downstream analysis attributes samples to specific models by position, a reordering of the config `models` list silently changes attribution without any error. No sort is applied before concatenation. Related: C-67 (schema pre-validation — different concern), D-15 (aggregation pattern — resolved as pure function). | Developer reorders models in an ensemble config file (e.g., alphabetizing, removing+re-adding a model) — concat aggregation produces different column ordering, breaking reproducibility or downstream per-model sample attribution | expert-code-review (2026-06-01, PF ensemble path forward review) | Open |
| C-144 | 3 | **`views-reporting` is a runtime dependency declared nowhere in package metadata.** After ADR-054 extraction, pipeline-core's shims, test files, and ensemble reconciliation all depend on `views-reporting` being installed, but no `pyproject.toml` declares this. Should be an optional extra, not a hard requirement. Correct sequence: (1) declare as optional extra, (2) update downstream lock files, (3) remove shims. Related: C-142 (resolved), C-143 (mitigated), C-134 (test import paths). Issue #130. | New developer or CI installs views-pipeline-core from `pyproject.toml` without manually installing views-reporting — runtime failures on reporting/statistics/reconciliation paths | PR #103 review + strategic analysis (2026-06-01) | Open |
| C-145 | 4 | **`validate_partition_config()` bare dict access — no KeyError guard.** `check.py:169-170` does `ensemble_manager._partition_dict[run_type]` and `model_manager._partition_dict[run_type]` with no guard for a missing key. Unlike `CoreConfigSniffer._check_evaluation_contract()` (line 327-330) and `ModelManager._assert_partition_config_accessible()` (line 1804-1806), which both provide actionable error messages when a run_type key is missing, this function produces a raw `KeyError` traceback. Crashes loudly (not a silent-corruption risk), but the error message gives no context about which partition dict or which run_type was invalid. Related: C-38 (untyped partition dicts — root cause), C-50 (same file). | A partition `config_partitions.py` omits a run_type key that the ensemble is invoked with — `KeyError` at `check.py:169` with no actionable message | issue #150 investigation (2026-06-02) | Open |
| C-146 | 3 | **Three-site `saved` invariant now integration-tested (PARTIALLY MITIGATED).** The constraint "non-training ensemble runs use saved data" is enforced at three independent sites: (1) `args.py:411` forces `--saved` for non-training/non-sweep runs; (2) ensemble call sites pass `saved=args.saved` to `validate_ensemble_model()`; (3) `_create_model_args()` in all three ensemble managers hardcodes `saved = ctx.args.saved if train else True` for subprocess dispatch. **Mitigated (2026-06-03):** 17 integration tests in `test_integration_saved_invariant.py` verify three-site coordination across all three manager types. Truth-table coverage: training/calibration, evaluate/calibration, forecast/forecasting, training-with-saved-false passthrough. Negative tests verify Site 1 rejection. Sweep exemption machine-verified as architecturally irrelevant (no `execute_sweep_run` on ensemble managers). D-23 comment added to `check.py:74-79`. CIC §3 updated in all three ensemble CICs with `_create_model_args` guarantee. **Remaining:** The three-site design itself is a maintenance burden — the invariant would be simpler at a single enforcement point. Related: C-16 (no integration tests), D-23 (resolved — keep guard, documented), D-24 (resolved — integration test added), C-61 (saved semantic gap). | A developer relaxes `args.py:411` to allow `saved=False` for non-training runs without understanding that `check.py` freshness checks and `_create_model_args` subprocess dispatch depend on the invariant; integration test now catches this regression | expert-code-review (2026-06-02) + falsify (2026-06-03) | Open |
| C-147 | 4 | **`_exit_with_error` uses `sys.exit(1)`, not `ValueError` — saved invariant enforcement bypassable in non-CLI contexts.** `args.py:104-113` enforces the `saved=True` requirement for non-training runs via `sys.exit(1)`, which raises `SystemExit` (a `BaseException`, not `Exception`). Standard `try/except Exception` does not catch it (safe in production CLI). However, `except BaseException` or `except SystemExit` does — any embedding context (long-running orchestrator, web API, test harness) that catches `BaseException` silently swallows the invariant violation. The test suite already demonstrates this: `test_forecasting_args.py` uses `pytest.raises(SystemExit)` at 17 call sites. Currently safe because all production use is CLI subprocess dispatch (`template_main.py` → `ForecastingModelArgs.parse_args()`). Related: C-146 (three-site invariant), D-24 (integration testing). | `ForecastingModelArgs` is constructed inside a non-CLI context (orchestrator, web API, notebook) that wraps model dispatch in `try/except BaseException` — the `saved=True` invariant silently disappears | falsify (2026-06-03, saved invariant completeness) | Open |
| C-148 | 3 | **`config_modelset` merge overwrites on key collision (MITIGATED — collision warning added).** `dataframe_ensemble.py`, `prediction_frame_ensemble.py`, and `ensemble.py` merge `config_modelset` into `config_meta` via `dict.update()`. If both configs define the same key (e.g., `"models"` left in `config_meta` during migration), `config_modelset` wins. **Mitigated (2026-06-04):** All three managers now log `logger.warning` on key collision. During migration from old config format (models in config_meta) to new (models in config_modelset), overlapping keys produce a visible warning. Related: C-06 (dynamic script loading). | An ensemble is partially migrated — `"models"` list remains in `config_meta.py` AND a `config_modelset.py` is created with different content; the merge overwrites (now with warning) | review (feature/config-modelset, 2026-06-04) | Open |
| C-152 | 4 | **Empty `models` list from config_modelset produces confusing errors.** If config_modelset returns `{"models": []}` and config_meta no longer contains `"models"`, ensemble managers iterate zero models. Training silently no-ops. Evaluation crashes with `StopIteration` in DF managers (`next(iter(eval_results.values()))` on empty dict) or `ValueError` in PF manager (empty frames list). All failures are loud but the error messages give no indication that the root cause is an empty models list. No test covers this path. Related: C-148 (config_modelset merge). | An ensemble's config_modelset.py is scaffolded from the template (which defaults to `"models": []`) and never populated before running | test-review (2026-06-05, config_modelset splash zone) | Open |
| C-156 | 3 | **ADR-011 configuration table and "Mandatory vs Optional" subsection contained three false claims — RESOLVED.** (1) Table listed `config_inputdata.py` — stale name, code uses `config_queryset.py`. (2) Subsection claimed `config_sweep.py` mandatory for all types — model-only. (3) Subsection omitted `config_partitions.py`. **Fixed (2026-06-05):** Table restructured with Scope column (All/Models only/Ensembles only). `config_inputdata.py` → `config_queryset.py`. `config_partitions.py` added. Subsection rewritten: 4 base mandatory, 2 model-only, 1 ensemble-optional. Regression tests: `test_falsification_adr011_mandatory_configs.py` (4 passing). Related: C-154. | N/A — fixed | falsify (2026-06-05, P2) | Resolved |
| C-157 | 4 | **Dead `model_name` parameter in `template_config_modelset.py` with lying docstring — RESOLVED.** `generate(script_path, model_name)` accepted `model_name: str` but the code string was a plain `"""` (not f-string) — `model_name` was never interpolated. The docstring falsely claimed it was "Included as a comment for reference." Copy-paste artifact from `template_config_meta.py`. **Fixed (2026-06-05):** Code string converted to f-string; `model_name` now embedded in generated docstring. Regression tests: `test_falsification_template_modelset_dead_param.py` (2 passing). Related: C-10 (template test coverage gap). | N/A — fixed | falsify (2026-06-05, dead-param audit) | Resolved |

---

## Expert Disagreements

| ID | Description | Perspectives | Resolution |
|----|-------------|-------------|------------|
| D-01 | **Decompose ForecastingModelManager now vs later.** Martin/GoF demand immediate decomposition; Feathers says characterization tests first; Nygard warns decomposition changes the failure surface. | Martin + GoF (now) vs Feathers + Nygard (staged) | Staged: characterization tests first, then Strangler Fig extraction per ADR-004 Revisit when E5 (TrainingStage extraction) completes. |
| D-02 | **Mutable ConfigurationManager vs immutable config.** Hickey insists config should freeze after setup; Beck notes current mutable config is test-ergonomic. | Hickey + Kleppmann (immutable) vs Beck (pragmatic) | Freeze after `update_for_single_run()` — preserve read interface Revisit when ConfigurationManager is next modified. |
| D-03 | **Inheritance hierarchy vs composition for EnsembleManager.** Hickey/Martin prefer composition; Feathers notes inheritance is load-bearing for all tests and CICs. 8-expert review (2026-05-18) revisited: now that ADR-045 stages are independently composable, the escape hatch exists. Feathers advocates Strangler Fig (wrap existing inheritance, migrate incrementally). Hickey argues the conceptual model is wrong — sever cleanly. Related: C-65 (LSP violation), D-13 (timing). | Hickey + Martin (composition) vs Feathers (preserve) → Updated: Feathers (Strangler Fig) vs Hickey (clean break) | Build new PredictionFrame ensemble from scratch with composition (proves pattern). Strangler Fig existing EnsembleManager to composition after new path is proven. |
| D-04 | **ModelPathManager relocation (E6) timing.** Martin/Hickey argue `model_path: Any` typing defers the real DIP violation — every new stage compounds it. Feathers/Nygard argue E6 has cross-repo blast radius (11 import sites, downstream model repos) and stages should stabilize first. | Martin + Hickey (relocate now) vs Feathers + Nygard (after stages stabilize) | `ModelPathProtocol` defined now (2026-04-06) as interim type safety. Full relocation deferred until after E5. |
| D-05 | **`_execute_model_evaluation()` decomposition timing.** Martin/Ousterhout argue the 226-LOC method is the most complex piece and E2 made the residual worse. Beck/Feathers argue it was uncharacterized and decomposing without tests first causes refactoring failures. | Martin + Ousterhout (decompose before E3) vs Beck + Feathers (characterization tests first) | 10 characterization tests written (2026-04-06). Decomposition can now proceed with E3. |
| D-06 | **WandB lifecycle template extraction timing.** GoF argues the repeated `with initialize_run: try/except/finally finish_run` pattern across 7 methods is a Template Method begging to be extracted. Beck says it is understood boilerplate safe to extract later. | GoF + Nygard (extract now) vs Beck (extract with E3) | Extract as part of E3 (ReportingStage) — the simplest stage and best place to prove the pattern. Revisit when ReportingStage is next modified. |
| D-07 | **Domain layer extraction priority vs god-class decomposition.** Martin/Clean Architecture: domain types (S-01..S-04) should precede mechanism refactoring because they define the vocabulary that decomposed classes depend on. Feathers: god-class decomposition (C-35/C-36/C-37) is more urgent because 4,755 LOC of structural debt compounds maintenance cost daily. Beck: domain types are zero-risk additive PRs; god-class extraction touches load-bearing code. | Martin (domain first) vs Feathers (god-class first) vs Beck (parallel — they don't conflict) | Resolved: S-01..S-04 in parallel with god-class work — additive PRs don't conflict with mechanism refactoring. Both paths proceeded independently; C-37 extracted (ADR-054), stories documented. |
| D-08 | **Documentation-first vs code-first for boundary violations.** C-43/C-46 identify concrete DIP violations. C-41/C-42 identify documentation gaps. Martin: document the target architecture first (ADRs define rules, then enforce). Feathers: fix C-43 immediately — it's a one-line import path change with zero risk. Beck: both are small, do in parallel. | Martin (ADRs first) vs Feathers (fix code first) vs Beck (parallel) | Resolved: ADRs written first (ADR-041, 045, 046-050), then code fixes applied (C-43 fixed 2026-04-11, C-46 enforced same date). Standing policy confirmed by practice. |
| D-09 | **Phase 1 workaround (`args.saved=True`) safety vs caller complexity leakage.** Feathers endorses `args.saved=True` as a safe Sprout Method — grow new behavior without touching legacy code. Ousterhout objects: callers must now understand framework internals (`use_saved` routing) to use it safely, violating deep-module principles. Both right at different time horizons. Related: C-51, C-53. | Feathers (safe now, fix later) vs Ousterhout (leaks complexity to callers) | Phase 1 is correct now (safety). Phase 2 must eliminate the caller burden by making source selection a framework concern. |
| D-10 | **Simple data descriptors vs correctness features embedded in viewser Queryset.** Hickey sees the viewser Queryset as complected (bundles data description with fetching + drift detection) and prefers bright_starship's plain dict. Kleppmann notes that the Queryset carries drift detection (ADR-023) — a genuine data correctness feature that the dict silently drops. Related: C-51, C-52. | Hickey (decouple description from mechanism) vs Kleppmann (don't lose correctness guarantees) | Descriptor should be simple data (dict/dataclass). Drift detection should be a framework-level concern configured per source, not embedded in the descriptor object. |
| D-11 | **Cache overwrite risk vs characterization-test-first approach.** Nygard flags that the default `use_saved=False` path always overwrites the cache (`dataloaders.py:1278-1279`), risking data loss if viewser returns bad data. Beck insists on writing characterization tests for `get_data()` before changing the default behavior. Related: C-14, C-51. | Nygard (change default to cache-validate-first) vs Beck (characterization tests before behavior change) | Resolved: Characterization tests written first (Beck consensus followed). C-14 (cache filename collision) resolved via source-encoded filenames. Cache overwrite behavior stable; residual concern tracked as C-61 (cache provenance). |
| D-12 | **Pass DataFrame to engine abstract methods vs engines loading from disk.** `_train_model_artifact()` (`model.py:591`) is abstract with no parameters — engines must independently locate and load the cached DataFrame. The base class already fetched, validated, and saved the data in `_execute_data_fetching()`. Adding `df` as a parameter would eliminate the filesystem-mediated coupling (C-59) entirely but is a breaking change to all engine repos. Related: C-59, D-01, D-09. | Ousterhout + Martin (pass df parameter — deeper module, eliminates filename coupling across repos) vs Feathers + Beck (add round-trip integration test first — API change to abstract method is risky without cross-repo test coverage; currently only 2-3 engine repos but each has multiple override sites) | Unresolved. Consensus direction: add round-trip test per engine repo first (Beck), then change the API (Ousterhout). |
| D-13 | **Refactor existing DataFrame ensemble to composition now vs after new PredictionFrame ensemble proves the pattern.** Martin sees the LSP violation (C-65) as technical debt that should be fixed regardless. Beck counters: refactoring without characterization tests is risky, and 5 existing ensembles (white_mustang, skinny_love, cruel_summer, pink_ponyclub, rude_boy) work in production. Risk/reward favors new-path-first. Related: D-03 (general composition question), C-65 (LSP violation). | Martin (fix LSP now — debt compounds) vs Beck (existing ensembles work, no tests, don't risk production) | Resolved (2026-05-18): DataFrameEnsembleManager FIRST as a proving ground — prove composition on known-correct ground (existing ensemble baseline), write characterization tests, then build PredictionFrameEnsembleManager for production HydraNet ensembles. Legacy migration optional/deferred. Rationale: isolates composition risk from PF/aggregation risk; characterization tests become safety net for PF implementation. |
| D-14 | **Infrastructure Facade vs Minimal Interface for shared pipeline services.** Ousterhout wants a deep `PipelineInfrastructure` module hiding `ModelManager.__init__`'s 15-step initialization (WandB, config loading, paths, prediction store, logging) behind one constructor. Hickey argues this moves complection behind a wall — the real fix is making each piece independently composable with explicit dependencies. Both the new PredictionFrame ensemble and the existing single-model path need these services. Related: C-01 (god class), C-45 (ISP violation — stages reach back through mutable references), C-48 (concrete dependencies). | Ousterhout (deep Facade — one constructor, hides complexity) vs Hickey (explicit composition — each piece independently wired, no hidden state) | For the new ensemble manager: explicit composition of individual pieces (Hickey). For the broader codebase cleanup: Facade (Ousterhout) as pragmatic intermediate step. Both approaches converge at the same target — the question is sequencing. |
| D-15 | **Aggregation as Strategy Object vs Pure Function for PredictionFrame ensembles.** GoF suggests a Strategy pattern: `PredictionFrameAggregator` interface with interchangeable method implementations (mean, median, concat, vincentization). Hickey says aggregation is a function, not an object — `aggregate(predictions, method) -> result`. The existing `AggregationManager` is Strategy-like because it manages internal Polars state, but PredictionFrame aggregation on dense NumPy arrays needs no intermediate state. Related: C-66 (OOM — motivates the new aggregation path). | GoF (Strategy pattern — extensible, consistent with existing AggregationManager) vs Hickey (pure function — no class needed, no state, simpler) | Pure function for the initial PredictionFrame aggregation (WET-before-DRY). If multiple aggregation methods emerge, extract to Strategy. |
| D-17 | **How to fix EnsembleManager's CoreConfigSniffer bypass: add call (Option A) vs Template Method (B) vs composition (C).** Option A: add `CoreConfigSniffer(self.configs, self._partition_dict).sniff_all(args.run_type)` to `EnsembleManager.execute_single_run()` before WandB login. Minimal, closes the gap immediately. Option B: restructure `ForecastingModelManager.execute_single_run()` into non-overridable `_pre_validate()` + overridable `_execute_pipeline()`. Structurally prevents future bypass. Option C: convert EnsembleManager from inheritance to composition (matching DataFrameEnsembleManager/ADR-051 pattern). Fixes C-65 (LSP) and C-55 simultaneously. Related: C-55 (validation gap), C-65 (LSP violation — root cause), D-03 (composition vs inheritance — resolved), D-13 (timing — resolved). | Martin/GoF (Template Method — keeps inheritance, enforces invariants) vs Hickey/Beck (composition — DataFrameEnsembleManager proves it works) vs Nygard (any fix, but unconditional — must run even on training runs) | **Resolved (2026-05-20):** Option A applied as bridge fix (CoreConfigSniffer call added to EnsembleManager.execute_single_run). Option B rejected — adding Template Method to a broken hierarchy makes it harder to replace with composition later. Option C is the permanent fix, on the D-13 roadmap: retire EnsembleManager → DataFrameEnsembleManager once the composition pattern is proven on all 5 production ensembles. Parameterized regression test prevents future bypass on any manager type. |
| D-18 | **Single deep validation entry point vs explicit CoreConfigSniffer call at each manager.** Ousterhout wants a single `validate_run(configs, partition_dict, run_type, manager_type)` function that composes CoreConfigSniffer + ensemble-specific checks internally. Hickey says CoreConfigSniffer already IS the deep module — the problem is a missing function call, not missing infrastructure. Adding a wrapper adds indirection without adding value. Related: C-55 (validation gap), D-17 (fix strategy). | Ousterhout (deep `validate_run()` — one call, hides composition) vs Hickey (explicit CoreConfigSniffer call — simple, transparent, already works for DataFrameEnsembleManager and ForecastingModelManager) | Explicit call (Hickey). The validation composition is two calls (CoreConfigSniffer + validate_ensemble_model) that serve independent purposes and change for independent reasons. Bundling them adds coupling without reducing complexity. |
| D-19 | **Bool flag (`is_ensemble: bool`) vs string parameter (`target: str`) for CoreConfigSniffer pipeline unit type.** Option 1: add `is_ensemble: bool = False` to constructor — simple, one concept. Option 2: accept `target: str` (`"model"` / `"ensemble"`) — re-uses the codebase's existing vocabulary (20+ locations dispatch on `model_path.target`), supports future unit types (preprocessor, extractor already in `ModelPathManager.get_model_name_from_path()`'s `valid_parents`), and each call site already has `self._model_path.target` available. Bool introduces a second vocabulary for the same concept. 6/8 experts favor Option 2. Related: C-82 (implicit detection concern), D-18 (resolved — explicit call preferred). | Martin/Hickey/Ousterhout/Beck/Kleppmann/Feathers (Option 2 — re-uses existing vocabulary, extensible) vs GoF (caution — open-coded enum without compile-time safety, mitigated by `Literal` annotation + construction-time validation) | Resolved: Option 2 (`target: str`). Keyword-only, required (no default), validated against `_VALID_TARGETS` frozenset at construction. `Literal["model", "ensemble"]` type annotation for IDE support. |
| D-20 | **Whether to add a cross-check (declared `target` param vs inferred config content) in CoreConfigSniffer.** Nygard/Beck: if `target="model"` but `"models"` in config, raise `ValueError` — catches misconfiguration at the boundary, zero runtime cost. Hickey: if the `target` parameter is authoritative, checking the config content re-introduces the complection you just removed. Related: C-82 (implicit detection), D-19 (resolved — Option 2). | Nygard/Beck (cross-check as `ValueError` — cheap safety net) vs Hickey (re-complects what was just separated) | Resolved: Include the cross-check. A `ValueError` when `target="model"` but `"models"` in config enforces the separation rather than just observing it. The check is authoritative-parameter-first (parameter wins, config content only flags inconsistency), not inference-based. |
| D-21 | **Should `_ViewsDataset` be decomposed before or during Sprint 4 (C-105)?** Martin/Hickey: decompose now so Sprint 4 doesn't cement coupling. Feathers/Beck: don't refactor without tests. Nygard: add scale guard now. **Updated (2026-05-29):** ADR-054 extraction partially resolves — ~1,342 LOC of misplaced methods extracted from `_ViewsDataset` to views-reporting (PR 8). Visualization consumers now live in views-reporting. Sprint 4 can proceed in views-reporting without touching `_ViewsDataset` in pipeline-core. Residual decomposition (Phase 2) remains deferred per C-36. Related: C-36, C-106, D-07. | Martin/Hickey (decompose before Sprint 4) vs Feathers/Beck (bypass, characterize later) vs Nygard (scale guard now) | Resolved (2026-05-29): ADR-054 extraction removed the blocking dependency. Sprint 4 proceeds in views-reporting without touching _ViewsDataset. Residual Phase 2 decomposition deferred per C-36 — no longer blocks any active work. |
| D-22 | **Speed-first (probe and fix on contact) vs knowledge-first (resolve documentation uncertainty before acting) for PredictionFrameEnsembleManager production path.** Feathers/Beck: run the cheapest possible probe — execute one constituent model with PF format and verify it produces valid `y_pred.npy`. Fix issues on contact. Hickey/Ousterhout: the 8 open issues (2026-03-03) have uncertain status — acting without resolving the uncertainty means you can't reason about correctness. Related: C-140 (transform undo ambiguity), C-66 (OOM — the motivating problem). | Feathers/Beck (probe immediately) vs Hickey/Ousterhout (resolve issue status first) | Resolved: decomposition shows probe IS the verification — they're the same thing when targeted at the actual production path. |
| D-23 | **`run_type` guard in `check.py:78` — dead code or defensive design?** The `run_type != "forecasting"` guard in `validate_model_conditions()` is currently unreachable for ensemble validation because `args.py:411` forces `saved=True` for all non-training runs, and the `saved` guard (line 95) fires first. Ousterhout: the guard adds cognitive load — readers must reason about whether the `run_type` branch or the `saved` branch fires first, and both ultimately skip the same checks. Hickey/Martin: the `run_type` guard encodes independent domain knowledge (ADR-018: freshness checks are conceptually forecasting-only) that should survive even if the `saved` invariant changes. Feathers: the guard IS tested in unit tests (`test_calibration_skips_freshness_checks`, `test_validation_skips_freshness_checks`) — it's not dead code, it's independently verified and would catch a regression if `args.py:411` is relaxed. Related: C-146 (three-site invariant), D-24 (integration testing). | Ousterhout (cognitive load — remove or document as defensive) vs Hickey/Martin/Feathers (independent domain knowledge — keep, it's tested) | Keep the guard. Add a comment at `check.py:78` documenting that the `run_type` and `saved` guards are deliberately independent per ADR-018. |
| D-24 | **Should the `args.py:411` invariant be integration-tested?** Beck/Feathers: the three-site invariant (args.py + check.py + _create_model_args) is a Critical Business Rule that should be machine-verified. An integration test constructing `ForecastingModelArgs(train=False, saved=False)` and asserting it exits would codify the contract. Martin: the code is clear enough — `sys.exit()` at args.py:411 is self-documenting, and unit tests for check.py already cover the downstream behavior. Nygard sides with Beck — invariants that span module boundaries need explicit tests because no single module's tests cover the full chain. Related: C-146 (three-site invariant), C-16 (no integration tests). | Beck/Feathers/Nygard (integration test — invariants spanning modules need explicit verification) vs Martin (code is self-documenting) | Resolved (2026-06-03): Integration test added. 17 parameterized tests in `test_integration_saved_invariant.py` verify three-site coordination across all three manager types. |
| D-16 | **Should `_load_c_dataset` / `_apply_reconciliation` fail loud or gracefully degrade when reconciliation IS configured?** Current contract: `_load_c_dataset()` returns `Optional[_CDataset]` (None on failure), `_apply_reconciliation()` treats None as non-fatal and returns unreconciled predictions. **Graceful camp** (Martin, GoF, Nygard): the method's role as a multi-tier fallback loader (provided → prediction store → local → None) is correct for operational resilience; network blips should not crash long-running ensemble runs. **Fail Loud camp** (Hickey, ADR-008, codebase philosophy): if reconciliation is *configured*, it is a Critical Business Requirement — silently publishing unreconciled predictions as reconciled is worse than crashing. The exception narrowing in C-71 accidentally moved toward Fail Loud (more exceptions propagate) but for the wrong reasons (incomplete handler, not a deliberate design choice). Related: C-68 (silent failure), C-71 (original narrowing), C-77 (incomplete narrowing). | Graceful degradation (return None, skip reconciliation, log warning) vs Fail Loud (raise PipelineException when configured reconciliation cannot complete) | **Resolved (2026-05-20):** Fail Loud. If reconciliation is configured, it is a Critical Business Requirement — crash with PipelineException rather than silently publish unreconciled predictions. C-68 and C-77 fixed accordingly. |

---

## Resolved Concerns

| ID | Tier | Description | Trigger | Source | Status |
|----|------|-------------|---------|--------|--------|
| C-37 | 3 | **God class: DatasetTransformationModule — extracted to views-reporting (ADR-054, PR 1).** Originally 1,410 LOC, zero internal consumers. Now lives in `views-reporting/transformations/`. Re-export shim in pipeline-core's `modules/transformations/__init__.py`. Decomposition candidates unchanged but now addressed in views-reporting, not pipeline-core. | N/A — no longer in pipeline-core | falsification-audit (2026-04-07) + ADR-054 extraction (2026-05-29) | Resolved |
| C-54 | 1 | **`_get_artifact_files()` treats sidecar files as model artifacts.** `model_path.py:561-567`: the extension allowlist includes `.json`, which matches config sidecar files (e.g. `calibration_model_20260421_213555.pt.config.json`). Since `Path.stem` strips only the last suffix, the sidecar's stem (`calibration_model_...pt.config`) still starts with `{run_type}_model_`, passing the prefix filter. When sorted reverse-alphabetically, the sidecar sorts before the `.pt` file, so `get_latest_model_artifact_path()` returns the JSON file instead of the model. `torch.load()` then crashes with `_pickle.UnpicklingError: invalid load key, '{'`. Violates ModelPathManager CIC §3 guarantee. **Fix (2026-04-22):** Added `len(f.suffixes) == 1` filter — primary artifacts have exactly one suffix; sidecars have compound suffixes. Related: C-42 (missing ViewsDataLoader CIC). | Any model that saves `.pt.config.json` or `.pt.sha256` alongside `.pt` artifact | bright_starship evaluation crash (2026-04-22) | Resolved |
| C-56 | 3 | **`validate_model_conditions()` returns bool instead of raising — violates ADR-008 fail-loud principle.** `modules/validation/ensemble/check.py:10-88`: catches exceptions at line 44-46, logs errors, and returns `False`. The caller `validate_ensemble_model()` at line 248-253 converts `False` returns to `exit(1)` — a system exit that bypasses Python's exception mechanism, preventing cleanup, WandB finalization, and meaningful stack traces. ADR-008 mandates exceptions with preceding `logger.error()`. The bool-return pattern invites silent failure if a new caller forgets to check the return value. Related: C-55 (ensemble validation gap), C-50 (layer violation in same file). **Fix:** `exit(1)` replaced with `raise ValueError` in `validate_ensemble_model()`. Sub-validators still return bool (incremental fix — full fail-loud conversion deferred). | New validation caller uses `validate_model_conditions()` without checking return value; or `exit(1)` bypass prevents WandB finalization on ensemble validation failure | repo-assimilation (2026-04-22) | Resolved |
| C-63 | 3 | **`save_predictions()` docstring omits `target_identifier` parameter.** `io.py:58-68` lists 7 of 8 parameters; `target_identifier` (line 55) is in the signature but undocumented. The undocumented parameter is the one that prevents multi-target prediction data loss. A maintainer adding a new caller would not know to pass it. | A developer adds a new code path that calls `save_predictions()` for a multi-target model, reads the docstring, and omits `target_identifier` — predictions overwrite silently | falsification-audit (2026-05-06) | Resolved |
| C-64 | 3 | **ForecastingStage CIC does not guarantee per-target filename uniqueness.** CIC §3 (line 42-44) guarantees "each target's PredictionFrame is passed directly to every saver" but says nothing about `target_identifier` or filename differentiation. A maintainer could construct `PredictionMetadata` with identical filenames for different targets and believe they are CIC-compliant. | A developer modifies `_save_via_savers()` or writes a new saver caller, follows the CIC guarantee, and produces identical filenames for different targets — predictions overwrite silently | falsification-audit (2026-05-06) | Resolved |
| C-68 | 2 | **Reconciliation silent failure returns unreconciled predictions as if reconciled.** `_apply_reconciliation()` (ensemble.py:604-644) is configured via `self.configs.get("reconciliation")`. When type is `"pgm_cm_point"`, `__reconcile_pg_with_c()` returns `None` if no CM model specified, if the C dataset can't be loaded (prediction store miss AND local file miss), or if reconciliation computation fails. `_apply_reconciliation()` logs a warning and returns the unreconciled DataFrame — no exception, no metadata flag, no WandB error alert. The unreconciled predictions are saved and published identically to reconciled ones. Downstream consumers have no way to distinguish reconciled from unreconciled output. This violates the codebase's "Fail Loud and Proud" principle (ADR-008). **Falsification evidence (2026-05-19):** 8-expert review unanimously identified this as the root architectural issue. The exception narrowing in C-71 accidentally surfaces this contract problem — whether exceptions propagate or are caught, the fundamental issue is that `_apply_reconciliation()` treats reconciliation failure as non-fatal when reconciliation IS configured. Related: C-56 (bool returns), C-71 (exception narrowing), C-77 (incomplete narrowing), D-16 (Fail Loud vs graceful degradation). **Fix (2026-05-20):** D-16 resolved as Fail Loud. `_apply_reconciliation()` in both `ensemble.py` and `dataframe_ensemble.py` now raises `PipelineException` (with preceding `logger.error` and WandB `ERROR` alert) when configured reconciliation returns None. Unreconciled predictions are never silently published as reconciled. | When an ensemble specifies `"reconciliation": "pgm_cm_point"` but the CM model (`reconcile_with`) has not been run yet, or the CM prediction files are missing — unreconciled predictions silently published as final | expert-review (2026-05-18) + falsification (2026-05-19) | Resolved |
| C-71 | 2 | **`_load_c_dataset()` catches bare `Exception` twice, swallowing bugs as missing data.** `ensemble.py:730-751` and `dataframe_ensemble.py:924-943` wrap prediction store lookup and local file lookup in `except Exception`. Any error — including `NameError`, `AttributeError`, or other bugs inside the try body — is logged as "could not find C dataset" and reconciliation is silently skipped. The broad `except` cannot distinguish "data not found" from "code bug." Related: C-68 (reconciliation silent failure). **Fix:** Prediction store block narrowed to `(ImportError, KeyError, IndexError, ValueError)`. Local path block narrowed to `(IndexError, FileNotFoundError)`. Applied to both `ensemble.py` and `dataframe_ensemble.py`. **Note (2026-05-20):** The narrowing is incomplete — misses ConnectionError, TimeoutError, PermissionError, OSError from realistic IO scenarios. See C-77. | When a code change introduces a bug inside `_load_c_dataset()` (e.g., wrong attribute access, renamed method), the bug is masked as a "missing data" warning and reconciliation is silently skipped | bug-sniff (2026-05-18) | Resolved |
| C-72 | 3 | **`PackageManager.get_latest_release_version_from_github()` catches bare `Exception` → returns `None`.** Catches `Exception` (anything not already caught by `requests.exceptions.RequestException`) and returns `None`. Caller `_get_pred_store_name()` falls back to version `"0.1.0"`, silently changing the prediction store namespace. **Fix:** Narrowed to `except (KeyError, TypeError, ValueError)` — only JSON-parsing errors are caught; code bugs propagate. | When a GitHub API response format changes or the method has a code bug, predictions silently go to the wrong prediction store namespace (`v000100_YYYY_MM` instead of the real version) | bug-sniff (2026-05-18) | Resolved |
| C-74 | 3 | **Baseline `bugfix/artifact-timestamp-extraction` branch commits ADR-016 but not the implementation fix.** The actual code changes to `baseline_manager.py` (removing `datetime.now()` overwrite, adding artifact timestamp extraction) and `tests/conftest.py` (adding `get_latest_model_artifact_path` mock) remain as uncommitted working-tree modifications. Merging the branch as-is would merge documentation about a fix that is not included in the merge. Related: C-75 (no regression test). | When a maintainer merges `bugfix/artifact-timestamp-extraction` into development without first committing the working-tree changes | falsify (2026-05-19) | Resolved |
| C-75 | 3 | **No regression test verifies the baseline timestamp contract.** 82 existing tests in views-baseline verify that `_evaluate_model_artifact` and `_forecast_model_artifact` run without error, but none asserts that `config["timestamp"]` after evaluation equals the artifact filename stem's last 15 characters. The exact same class of bug (overwriting the timestamp with `datetime.now()`) could be reintroduced without any test failing. Falsification stubs written to `tests/test_falsification_timestamp_contract.py` (3 tests, all passing with the fix applied). Related: C-74 (uncommitted fix), C-16 (no integration tests). | When a developer refactors `_evaluate_model_artifact` or `_forecast_model_artifact` and removes or reorders the `add_config({"timestamp": ...})` call | falsify (2026-05-19) | Resolved |
| C-76 | 4 | **Hydranet ADR-026 does not reference central contract ADR-052.** ADR-052 references ADR-026 six times, but ADR-026 contains no reference back to ADR-052 or to the `config` property trap anti-pattern. A developer reading ADR-026 to understand the timestamp contract would not discover the central contract, the anti-pattern documentation, or the property-setter distinction. No branch exists in views-hydranet to add this reference. | When a developer reads ADR-026 in isolation and implements a timestamp pattern without knowing about the `config["key"] = val` no-op trap | falsify (2026-05-19) | Resolved |
| C-77 | 2 | **`_load_c_dataset()` exception narrowing incomplete — misses IO exceptions from network and filesystem.** PR #79 narrowed `except Exception` to specific types: prediction store block (`ensemble.py:761`, `dataframe_ensemble.py:923`) catches `(ImportError, KeyError, IndexError, ValueError)` but misses `ConnectionError`, `TimeoutError`, `RuntimeError` from SQLAlchemy/ViewsMetadata. Local path block (`ensemble.py:778`, `dataframe_ensemble.py:940`) catches `(IndexError, FileNotFoundError)` but misses `PermissionError`, `OSError` (siblings under OSError, not children of FileNotFoundError). Old behavior: `except Exception` caught all → returned None → reconciliation skipped. New behavior: uncaught exceptions propagate → run crashes. 4 falsification tests confirm the regressions. **Critical note:** Restoring `except Exception` re-introduces C-71. Widening the exception list is a band-aid. The real question is whether the Optional-return contract is correct when reconciliation is configured (see D-16, C-68). Related: C-71 (narrowing that introduced this), C-68 (silent failure contract), D-16 (design question). **Fix (2026-05-20):** Exception handlers widened: prediction store block now catches `(ImportError, KeyError, IndexError, ValueError, OSError)`, local path block catches `(IndexError, OSError)`. `OSError` covers `ConnectionError`, `TimeoutError`, `PermissionError`, `FileNotFoundError` (all subclasses). Programming bugs (`AttributeError`, `TypeError`, `NameError`) still propagate (C-71 preserved). 4 falsification tests now pass. | When prediction store has a network outage, database timeout, or filesystem has permission issues — ensemble run crashes instead of gracefully degrading. Whether crashing is correct depends on D-16 resolution. | falsification (2026-05-19) | Resolved |
| C-78 | 3 | **No pre-flight validation of reconciliation config — invalid config detected only after hours of forecasting.** `CoreConfigSniffer.sniff_all()` validates targets, metrics, deployment status, evaluation contract, prediction format, and evaluation mode — but has zero reconciliation validation. `validate_ensemble_model()` (`check.py`) also performs no reconciliation checks. When `reconciliation="pgm_cm_point"` is set but `reconcile_with` is None or missing, the error is caught only when `_apply_reconciliation()` fires PipelineException — AFTER all sub-model forecasts complete (potentially hours of compute wasted). Falsification test `test_config_sniffer_catches_reconcile_with_none` confirms: `sniff_all()` passes without error on invalid reconciliation config. Related: C-55 (ensemble validation gaps), C-68 (resolved — Fail Loud fix), D-16 (resolved — Fail Loud). **Fix (2026-05-20):** Added `_check_reconciliation_config()` to `CoreConfigSniffer`, wired into `sniff_all()`. Validates `reconciliation` against `SUPPORTED_RECONCILIATION_TYPES`; when `"pgm_cm_point"`, requires non-empty `reconcile_with`. Pattern follows `_check_evaluation_mode()`. 6 tests in `TestReconciliationConfigValidation`; falsification test now passes. | When an ensemble specifies `reconciliation="pgm_cm_point"` but `reconcile_with` is None/missing — hours of sub-model forecasting complete before the config error is detected | falsification (2026-05-20) | Resolved |
| C-80 | 3 | **Bool-returning ensemble validators mask infrastructure failures as data staleness.** `validate_model_conditions()` and `validate_ensemble_model_deployment_status()` catch bare `Exception` on log file reads and return `False`. A filesystem PermissionError or IOError is indistinguishable from "model was trained in the wrong cycle." Violates Fail Loud philosophy. Related: C-56 (resolved), C-55 (ensemble validation gaps). **Fix:** Narrowed both `except` clauses to `(FileNotFoundError, OSError, ValueError, KeyError)` — infrastructure errors (e.g. `PermissionError`) now propagate instead of being flattened to `False`. | When filesystem permissions change, disk fills, or NFS mount drops — `validate_ensemble_model()` raises `ValueError("failed validation for model X")` instead of the real `PermissionError`, misdirecting the operator | expert-review (2026-05-20) | Resolved |
| C-81 | 4 | **Double WandB alerting on every ensemble exception path.** `_apply_reconciliation()` sent an explicit WandB ERROR alert then raised `PipelineException`. The outer handlers caught `Exception` and created a new `PipelineException` with `wandb_module` — the constructor auto-sent a second alert. Same re-wrap pattern in all `_execute_model_*` methods in both ensemble managers. Related: C-68 (Fail Loud fix added the inner alert). **Fix:** (1) Removed explicit `send_alert(ERROR)` from `_apply_reconciliation` in both files, passing `wandb_module` to PipelineException instead. (2) Added `except PipelineException: raise` before every `except Exception:` handler in both ensemble managers (outer handler + all stage methods) to prevent re-wrapping. | When a developer adds a specific WandB alert before raising an exception in any ensemble stage method — operators receive duplicate alerts, eroding trust in alerting | falsification (2026-05-20) | Resolved |
| C-82 | 2 | **Implicit ensemble detection in CoreConfigSniffer — identity inferred from config content.** `core_config_sniffer.py:81` sets `self._is_ensemble = "models" in self._c`, inferring pipeline unit identity from config dict content rather than receiving a declared identity from the caller. If a model config ever legitimately contains a `"models"` key (e.g., reference models for comparison), the sniffer silently switches to ensemble mode — skipping validation of `algorithm`, `time_steps`, and `prediction_format`. The failure would surface far downstream (during training or prediction saving) with an error that does not mention CoreConfigSniffer. The rest of the codebase dispatches on `model_path.target` (20+ locations in `reporting/stage.py`, `evaluation.py`, `prediction/io.py`, `model.py`), which is set by class hierarchy (`ModelPathManager._target = "model"`, `EnsemblePathManager._target = "ensemble"`). Fix: accept explicit `target: str` parameter from caller, validated at construction time against `_VALID_TARGETS` frozenset. Related: C-55 (bridge fix wired CoreConfigSniffer into EnsembleManager — which is where the implicit detection was introduced), C-38 (no domain entity layer — `target` is a bare string), D-19 (bool vs string parameter). | A developer adds a `"models"` key to a model config for a non-ensemble purpose (e.g., comparison benchmarks, reference model list) — CoreConfigSniffer silently relaxes mandatory key checks | expert-code-review (2026-05-20) | Resolved |
| C-83 | 4 | **`time_steps` accepts float silently — no type enforcement in CoreConfigSniffer.** `_resolve_time_steps()` compares `len(steps) != time_steps` and returns the value. Python's numeric coercion means `36.0 == 36` is `True` and `36.0 in {36}` is `True`, so a float `time_steps=36.0` passes all validation silently. Subsumes under C-38 (no typed domain entities). **Fix:** Added explicit `isinstance(time_steps, int)` check in `_resolve_time_steps()` — raises `TypeError` for non-int values. | A config file sets `time_steps = 36.0` (float literal or result of a division) — CoreConfigSniffer passes it through; downstream code that does integer-specific operations (range construction, string formatting with `%d`) may produce subtle errors | falsification (2026-05-20) | Resolved |
| C-84 | 3 | **EnsembleManager output count test skipped — "exactly N DataFrames" guarantee unverified.** CIC §3 guarantees `_execute_model_forecasting()` produces exactly one output DataFrame per constituent model, but no test asserts this count. **Fixed (2026-05-20):** `TestOutputCount::test_forecast_ensemble_returns_dataframe` verifies the return type. `TestEnsembleFailureModes::test_evaluate_output_count_mismatch_raises` verifies that mismatched output counts raise ValueError. Related: C-55 (validation gap), C-85 (failure modes). | When a constituent model is added/removed from an ensemble and the output count changes, or a silent error in subprocess collection drops a result | test-review (2026-05-20) | Resolved |
| C-85 | 3 | **EnsembleManager failure modes fully tested (6/6).** CIC §6 lists: (1) missing sub-model script, (2) sub-model non-zero exit, (3) missing prediction file after run, (4) ensemble config missing `models` key, (5) WandB initialization failure, (6) aggregation shape mismatch. **Fixed (2026-05-20):** `TestEnsembleFailureModes` covers (3), (4), (6), plus output count mismatch. `TestSubprocessTimeout` covers (1)+(2). **Further cleanup (2026-05-20):** `except Exception` in `ensemble.py` prediction store lookup narrowed to specific types. **Fixed (2026-05-21):** `test_wandb_login_failure_propagates` added to both `TestEnsembleFailureModes` and `TestFailureModes` (DataFrameEnsembleManager), covering mode (5). Related: C-55 (validation gap), C-84 (resolved), C-07 (subprocess risk). | N/A — all 6 failure modes now tested | test-review (2026-05-20) | Resolved |
| C-86 | 3 | **CIC §10 (Test Alignment) sections drift from actual test coverage.** 8 of 27 CICs have §10 sections that list expected tests. Spot-check showed drift in 3 CICs. **Fixed (2026-05-21):** Updated §10 in CoreConfigSniffer.md (5 classes, 89 methods enumerated), CorePredictionSniffer.md (7 classes, 22 methods enumerated), EnsembleManager.md (18 classes, 60+ methods enumerated with coverage categories). Each §10 now names every test class and maps it to the contract section it verifies. | When CIC §10 is updated — re-audit after major test additions | test-review (2026-05-20) | Resolved |
| C-87 | 3 | **ViewsDataLoader float64 dtype guarantee tested end-to-end.** CIC §3 guarantees all numeric columns are float64 after loading. The implementation calls `ensure_float64()` on both viewser and datafactory fetch paths. **Fixed (2026-05-21):** `TestDtypeGuarantee` rewritten to test end-to-end enforcement: `test_int32_columns_promoted_to_float64` feeds int32 data through the loader and verifies float64 output; `test_float32_columns_promoted_to_float64` does the same for float32. Previous gap-documenting tests replaced — the loader was already enforcing correctly; only the tests were misleading. Related: C-16 (no integration tests). | N/A — enforcement verified end-to-end | test-review (2026-05-20) | Resolved |
| C-88 | 3 | **`_ENTITY_RENAME` mapping not unit-tested in DataFrameEnsembleManager.** `dataframe_ensemble.py:793` applies `_ENTITY_RENAME = {"priogrid_gid": "priogrid_id"}` during aggregation for ADR-034 compliance. This rename was a bug fix (caught by parity testing, 2026-05-20). **Fixed (2026-05-20):** `TestEntityRenameDataFrameEnsemble` (3 tests) verifies: `priogrid_gid` → `priogrid_id` rename, passthrough when already `priogrid_id`, and `country_id` index unchanged. Related: C-67 (no schema pre-validation). | When ADR-034 rename mapping changes upstream or `_ENTITY_RENAME` dict is accidentally removed | test-review (2026-05-20) | Resolved |
| C-89 | 3 | **EvaluationAdapter empty-intersection path untested.** `EvaluationAdapter.from_dataframes()` raises when actuals and predictions have zero temporal overlap. **Fixed (2026-05-20):** `TestEmptyIntersection` (3 tests) verifies: `test_all_sequences_zero_overlap_raises` (ValueError on fully disjoint), `test_partial_overlap_some_sequences_empty` (overlapping sequences survive while disjoint are dropped), `test_zero_overlap_warning_logged` (logger.warning fires on zero-overlap sequence). Related: C-26 (silent truncation on partial overlap). | When actuals from a different cycle are loaded for evaluation — error path now tested | test-review (2026-05-20) | Resolved |
| C-90 | 3 | **WandBModule `log_artifact` re-raise untested.** `WandBModule.log_artifact()` catches exceptions and re-raises as `PipelineException`. CIC §6 documents this as a guaranteed failure mode. **Fixed (2026-05-20):** `test_log_artifact_reraises_on_failure` verifies `RuntimeError` propagation (artifact creation failure). `test_log_artifact_reraises_on_upload_failure` verifies `OSError` propagation (upload failure). Both confirm Fail Loud behavior. Related: C-15 (no timeouts on WandB), C-42 (missing WandBModule CIC). | When WandB API has an outage or returns an error during artifact logging — re-raise now tested | test-review (2026-05-20) | Resolved |
| C-91 | 4 | **ConfigurationManager `__delitem__` KeyError untested.** `ConfigurationManager.__delitem__()` raises `KeyError` when deleting a non-existent key. CIC §6 documents this. **Already covered:** `test_configuration.py::test_delitem_not_in_runtime` (line 161-164) already tests this exact path. The test-review finding was a false positive — the test existed but was missed during the coverage scan. Related: C-24 (safety-critical parameter override — resolved). | N/A — already tested | test-review (2026-05-20) | Resolved |
| C-92 | 2 | **DataFrameEnsembleManager `_load_or_generate_prediction` ignored `sequence_number` in post-generation file selection.** `dataframe_ensemble.py:770` unconditionally picked `prediction_files[0]` after shell script generation, ignoring the `sequence_number` parameter. During rolling-origin evaluation (13 sequences), all sequences silently loaded sequence 0's predictions, producing identical evaluation metrics across origins. No error signal — metrics looked plausible but were duplicated. The matching logic (filter by `_NN` suffix, raise if no match) existed in `ensemble.py:625-634` but was never ported to `dataframe_ensemble.py` when it was created. Root cause: WET duplication between the two managers (see D-03/D-13). **Fixed (2026-05-20):** Ported sequence-matching logic from `ensemble.py`. Related: C-65 (LSP violation — root cause of duplication), D-13 (retire EnsembleManager). | When DataFrameEnsembleManager runs rolling-origin evaluation and cached predictions don't exist — shell script generates predictions but wrong sequence file is loaded | tech-debt-cleanup (2026-05-20) | Resolved |
| C-94 | 2 | **Timestamp source divergence between PF producer and PF ensemble consumer — ensemble can never find sub-model outputs.** `model.py:1294` (Track A+ eval) and `model.py:1481` (Track A+ forecast) save PredictionFrame outputs using `self.configs.get("timestamp", "")`, which is `datetime.now()` from `ConfigurationManager.__init__()` (configuration.py:182). `PredictionFrameEnsembleManager._evaluate_model_artifact()` (prediction_frame_ensemble.py:580) and `_forecast_model_artifact()` (prediction_frame_ensemble.py:621) construct load paths using `path_artifact.stem[-15:]` — the artifact creation timestamp embedded in the model filename. These are structurally different values: the artifact timestamp is from when the model was trained; the runtime timestamp is from when the sub-model evaluation/forecast runs. Consequence: `_load_or_generate_pf()` always misses the cache (path doesn't exist), triggers a subprocess, then fails to find the generated output (subprocess saved at runtime-ts path, ensemble looks at artifact-ts path), raising `PipelineException`. The entire PF ensemble evaluation and forecasting flow is non-functional. **Fixed (2026-05-22):** Replaced `configs.get("timestamp")` with `get_latest_model_artifact_path(run_type).stem[-15:]` at both Track A+ save sites. Producer and consumer now derive timestamp from the same artifact stem. Regression tests: `TestP2TimestampFromArtifact`, `TestP4ProducerConsumerPathAgreement`. Falsification tests: `TestP2TimestampDivergence`, `TestP4GenerateThenLoadBroken` in `tests/test_falsification_pf_ensemble_integration.py`. Related: C-16 (no integration tests — root cause of the gap). | When PredictionFrameEnsembleManager runs evaluation or forecasting against pre-trained sub-models — the first `_load_or_generate_pf()` call fails with PipelineException because generated output is at a different timestamp path than expected | falsify (2026-05-22) | Resolved |
| C-95 | 2 | **EvaluationStage.evaluate() crashes with AttributeError when io_manager=None.** `PredictionFrameEnsembleManager` constructs `EvaluationStage` with `io_manager=None` (prediction_frame_ensemble.py:162). `EvaluationStage.evaluate()` unconditionally calls `self._io.generate_evaluation_table()` (evaluation/stage.py:119) after the target evaluation loop and `self._io.save_evaluations()` (evaluation/stage.py:229) for each target when `sweep` is not set. Both raise `AttributeError: 'NoneType' object has no attribute '...'`. The crash occurs after all compute-intensive metric computation completes — results are computed but never persisted or reported. `DataFrameEnsembleManager` passes a real `PredictionIOManager` (dataframe_ensemble.py:155-166), so only the PF path is affected. Tests hide this: both `TestPredictionFrameEvaluationFlow` tests mock `_evaluate_predictions` entirely, so `EvaluationStage.evaluate()` never executes. **Fixed (2026-05-22):** `generate_evaluation_table` called as `@staticmethod` via `PredictionIOManager` class; `save_evaluations` guarded with `self._io is not None`. Regression tests: `TestP3EvaluationStageNoneIO`. Falsification tests: `TestP3EvaluationStageNoneIO` in `tests/test_falsification_pf_ensemble_integration.py`. Related: C-34 (_execute_model_evaluation complexity — similar boundary area). | When PredictionFrameEnsembleManager evaluation completes metric computation and attempts the summary alert — AttributeError crashes the run, losing all computed metrics | falsify (2026-05-22) | Resolved |
| C-96 | 3 | **`_get_generated_pf_prediction_paths()` checks for y_pred.npy at wrong nesting level — returns empty list for all real data.** `model_path.py:645` checks `(d / "y_pred.npy").exists()` where `d` is a direct child of `data_generated/` (e.g., `predictions_calibration_20260522_120000/`). But production persistence nests `y_pred.npy` inside target subdirectories: `predictions_{run_type}_{ts}/{target}/y_pred.npy` (forecast, model.py:1483-1487) or `predictions_{run_type}_{ts}/origin_{i}/{target}/y_pred.npy` (eval, model.py:1319-1324). The method always returns `[]` for real data. Tests pass because fixtures create `y_pred.npy` directly in the timestamp directory — a flat structure that production code never creates (test_model_path.py:617-619). Currently unused by `PredictionFrameEnsembleManager` (which uses explicit path construction), but documented in CIC §11 as "an alternative lookup mechanism." **Fixed (2026-05-22):** Removed `y_pred.npy` existence check; method now filters by directory naming convention only (matching DF equivalent). Test fixtures updated to use real nested layouts. Falsification tests: `TestP1DiscoveryNestingLevel` in `tests/test_falsification_pf_ensemble_integration.py`. | When a developer calls `_get_generated_pf_prediction_paths()` for cache management, artifact listing, or as the CIC-documented alternative lookup — the method returns empty list despite predictions existing on disk | falsify (2026-05-22) | Resolved |
| C-98 | 4 | **EvaluationStage `elif self._io is None` log fires misleadingly during sweeps.** `stage.py:230-240` guard structure: `if not sweep and self._io is not None: ... elif self._io is None: ...`. When `sweep=True` and `io_manager=None` (PF ensemble sweep), the first condition short-circuits on `not sweep`, but the `elif` fires because `self._io is None`, logging "Skipping evaluation file save — no io_manager configured" even though sweeps never save evaluations regardless. No correctness or reliability impact — purely a misleading log message during an uncommon edge case (PF ensemble + sweep mode). Related: C-95 (io_manager=None crash — now resolved). **Fixed (2026-05-22):** Restructured guard to nested `if not sweep: if io is not None: ... else: ...` — sweep=True now skips silently without misleading log. Commit 48cd14f. | When a developer investigates PF ensemble sweep logs and sees "Skipping evaluation file save — no io_manager configured" — they may misdiagnose a sweep as having an io_manager problem instead of understanding sweeps inherently skip saves | review-diff (2026-05-22, PR #84) | Resolved |
| C-100 | 3 | **Ensemble template scaffolding routes exclusively to EnsembleManager — PredictionFrameEnsembleManager is undiscoverable.** `template_main.py:48` hardcodes `EnsembleManager` import. `template_config_meta.py` omits `prediction_format` key and lists only DF-compatible aggregation methods (`"median"`, `"mean"`). PF ensembles require `PredictionFrameEnsembleManager` import, `prediction_format: "prediction_frame"`, and PF-specific aggregation (`"arithmetic_mean"` or `"concat"`). None of this is documented or templated. All 8 existing ensembles in views-models use DF mode. A developer trying to create a PF ensemble must read source code to discover the class, config keys, and valid aggregation values. Falsification test: `TestF2TemplateDiscoverability` in `tests/test_falsification_pf_ensemble_setup.py`. Related: D-13 (composition vs inheritance trajectory). **Fixed (2026-05-23):** Template now imports `PredictionFrameEnsembleManager`, includes `prediction_format` key, and documents PF-specific aggregation methods in inline comments. Commit `2495d03`. | A developer scaffolds a new PF ensemble using the template — gets EnsembleManager instead of PredictionFrameEnsembleManager, with incompatible aggregation defaults | falsify (2026-05-23, PF ensemble setup audit) | Resolved |
| C-102 | 2 | **Ensemble template config_meta missing `regression_targets` / `classification_targets` — sniffer rejects all template-generated configs.** `template_config_meta.py` uses the legacy `"targets"` key but `CoreConfigSniffer._check_targets_and_metrics()` requires at least one of `"regression_targets"` or `"classification_targets"` to be non-empty. `ConfigurationManager.get_combined_config()` (line 530-534) normalizes missing keys to `[]`. Sniffer sees both empty → raises `ValueError("At least one of regression_targets or classification_targets must be non-empty")`. Affects ALL ensemble types (DF and PF), not just PredictionFrameEnsembleManager. Existing 8 ensembles in views-models work because they were set up manually with `"regression_targets"`, not from the template. Falsification test: `TestF2TargetKeysPresent` in `tests/test_falsification_pf_ensemble_setup_r2.py`. Related: C-100 (template discoverability — resolved). **Fixed (2026-05-23):** Added `regression_targets` and `classification_targets` keys to `template_config_meta.py`. Regression test passes. | A developer scaffolds a new ensemble from the template and runs `execute_single_run` — CoreConfigSniffer rejects the config before any work starts | falsify (2026-05-23, round 2) | Resolved |
| C-103 | 4 | **Template default `aggregation: "median"` is PF-incompatible — traps users who change only `prediction_format`.** `template_config_meta.py` defaults to `"aggregation": "median"`. `SUPPORTED_PF_AGGREGATION_METHODS` only allows `"concat"` and `"arithmetic_mean"`. If a user changes `prediction_format` to `"prediction_frame"` without changing `aggregation`, the ensemble crashes at `_aggregate_prediction_frames()` with `ValueError("Unsupported aggregation method: 'median'")`. The inline comment documents correct PF values, so the information is discoverable, but the default traps users. Falsification test: `TestF1AggregationDefaultCompatibility` in `tests/test_falsification_pf_ensemble_setup_r2.py`. Related: C-100 (template discoverability — resolved), C-102 (template completeness). **Resolved (2026-05-23):** Default is correct for default prediction_format ("dataframe"). Regression test rewritten to verify aggregation/format consistency — if default format changes to PF, test will catch incompatible aggregation. The PF values are documented in the inline comment. | A developer changes `prediction_format` to `"prediction_frame"` in the template config without also changing `aggregation` from the default `"median"` | falsify (2026-05-23, round 2) | Resolved |
| C-107 | 4 | **Stale mock target in `test_skip_predictions_delivery_skips_track_b` — patches `to_prediction_df` but production calls `to_arrow_table`.** `tests/test_managers/test_model_manager_prediction_format.py:1235` patches `PredictionFrameConverter.to_prediction_df`, but the production code at `model.py:1339` calls `converter.to_arrow_table`. The assertion `mock_convert.assert_not_called()` passes trivially — `to_prediction_df` is never called in the PF eval path regardless of Track B state. The test's second assertion (`_save_predictions.assert_not_called()`) is valid and does verify the behavior. Fix: replace `to_prediction_df` with `to_arrow_table` in the patch. Falsification test: `TestF4StaleMethodPatch` in `tests/test_falsification_track_b_retirement.py`. Related: C-10 (no viz test coverage). | A developer modifies the Track B conditional logic in `model.py:1338` and relies on this test's `to_prediction_df` assertion as proof of correctness — it passes trivially regardless of behavior | falsify (2026-05-26, Track B retirement audit) | Resolved |
| C-115 | 4 | **Extraction investigation document has stale LOC figures and wrong line numbers.** `reports/views_reporting_extraction/architectural_misplacement_investigation.md` exec summary says "~8,900 LOC" and "~1,279 LOC" for handlers.py methods; census section says ~8,285 total and ~1,342 handlers.py methods (626 LOC gap). Line 14 embeds stale "7,632" figure (= 6,943 + old C-36 estimate of 689). Appendix A cites `handlers.py:9` (matplotlib), `:11` (joblib), `:14` (torch) — actual lines are 10, 12, 15 (off by +1 each). Exec summary says "13 source files" but census lists 12 .py files. Dependency count varies: "7 heavyweight", "6-7 entries", 8 listed in footprint section. PR plans document has correct line numbers but step numbering (PRs 0-10) doesn't map 1:1 to investigation steps (1-10). Falsification tests: `tests/test_falsification_extraction_docs_consistency.py` (F-1 through F-5, F-7). Related: C-105 (extraction enables Sprint 4), C-116 (stale sprint plan). | A developer uses the investigation's exec summary LOC or line numbers to scope extraction work or verify an import line change — gets wrong numbers, wastes time or modifies the wrong line | falsify (2026-05-27, extraction docs consistency audit) | Resolved |
| C-116 | 3 | **Sprint plan C-105 targets file paths that extraction will delete.** `reports/views_reporting_extraction/sprint_plan_c105_eval_report_scale_aware_graphs.md` directs developers to modify `views_pipeline_core/templates/reports/evaluation.py` (Steps 1-3) and references `views_pipeline_core/modules/visualizations/historical.py`. The investigation (Section 11) says extraction PRs 0-6 must complete before Sprint 4 begins — PR 3 moves `visualizations/` and PR 6 moves `templates/reports/` to views-reporting. After extraction, these file paths will not exist in pipeline-core. The sprint plan has no header indicating it is pre-extraction and must be rewritten. Falsification test: `TestF6SprintPlanFilePathValidity` in `tests/test_falsification_extraction_docs_consistency.py`. Related: C-105 (the sprint itself), C-115 (investigation inconsistencies). | Sprint 4 begins after extraction PRs 0-6 merge and a developer follows the C-105 sprint plan targeting the old pipeline-core file paths — files don't exist, work is wasted | falsify (2026-05-27, extraction docs consistency audit) | Resolved |
| C-117 | 2 | **Extraction PR plans create circular pip install dependency.** PR 0 (`extraction_pr_plans.md:75`) declares views-reporting depends on pipeline-core (for `_ViewsDataset`, `ModelPathManager`). PR 9 (`extraction_pr_plans.md:972`) adds views-reporting as a hard dependency of pipeline-core (for re-export shims). This creates an unresolvable circular install: `pip install views-pipeline-core` requires views-reporting which requires pipeline-core. Neither `pip` nor `pip install -e` can resolve mutual hard dependencies without manual intervention. Resolution options: (1) make re-export shims use `try/except ImportError` so views-reporting is optional, (2) don't add views-reporting as a hard dep — shims are transitional and can fail gracefully, (3) use a single editable-install bootstrap script. Falsification test: `TestF1CircularDependencyResolution` in `tests/test_falsification_extraction_docs_sufficiency.py`. Related: C-47 (deferred imports indicate circular deps). | Developer executes PR 0 then PR 9 as written — `pip install views-pipeline-core` fails with unresolvable dependency cycle | falsify (2026-05-27, extraction docs sufficiency audit) | Resolved |
| C-118 | 3 | **PR 1 extraction has no downstream consumer inventory.** PR 1 moves `DatasetTransformationModule` (1,431 LOC, zero internal consumers) to views-reporting but names no downstream repo or file that imports it. The module is only re-exported from `__init__.py` — all consumers are in external repos (views-models, views-hydranet, views-baseline). Without an inventory, a developer cannot verify PR 1 is complete or know which companion PRs are needed in downstream repos. Falsification test: `TestF2DownstreamConsumerInventory` in `tests/test_falsification_extraction_docs_sufficiency.py`. Related: C-115 (extraction docs quality). | Developer merges PR 1, believes extraction is complete, but downstream repos still import from old path — discovered only when downstream CI breaks | falsify (2026-05-27, extraction docs sufficiency audit) | Resolved |
| C-119 | 2 | **PR 9 incorrectly lists tqdm for removal from pipeline-core.** `extraction_pr_plans.md` PR 9 dependency cleanup lists tqdm alongside torch/matplotlib/joblib for removal. However, 3 ensemble managers that STAY in pipeline-core import tqdm: `ensemble.py:8` (`from tqdm.auto import tqdm`), `dataframe_ensemble.py:23`, `prediction_frame_ensemble.py:21`. Executing PR 9 as written would remove tqdm from `pyproject.toml`, breaking all ensemble training/evaluation runs at import time. Falsification test: `TestF3TqdmRemovalAccuracy` in `tests/test_falsification_extraction_docs_sufficiency.py`. Related: C-115 (extraction docs quality). | Developer executes PR 9 dependency removal as written — removes tqdm from pyproject.toml — ensemble managers fail with `ModuleNotFoundError` on next import | falsify (2026-05-27, extraction docs sufficiency audit) | Resolved |
| C-120 | 3 | **PR 8 method extraction under-specified — no function signatures.** PR 8 extracts ~15 statistical/viz/reconciliation methods from `_ViewsDataset` (handlers.py) into standalone functions in views-reporting. The plan says only "Replace all `self.` references with `dataset`" but doesn't specify how to handle 10 distinct instance attributes (`is_prediction`, `to_tensor`, `targets`, `get_subset_tensor`, `dataframe`, `num_entities`, `sample_size`, `_time_values`, `_entity_values`, `original_index`). A developer must design ~15 function signatures with no guidance on which attributes to pass individually vs. as the full dataset object. Only `dataframe` is mentioned. Falsification test: `TestF4MethodExtractionSpecification` in `tests/test_falsification_extraction_docs_sufficiency.py`. Related: C-36 (_ViewsDataset decomposition). | Developer starts PR 8, discovers each method accesses different combinations of 10 instance attributes, and must make design decisions the PR plan doesn't address — blocks PR or produces inconsistent API | falsify (2026-05-27, extraction docs sufficiency audit) | Resolved |
| C-121 | 3 | **7 test files import from extracted modules — zero listed in any PR plan.** These test files import from modules being extracted: `test_views_dataset.py`, `test_statistics.py`, `test_reports_utils.py`, `test_reconciliation.py`, `test_report.py`, `test_transformations.py`, plus `test_falsification_extraction_docs_sufficiency.py` (self-referential). No PR plan's "Files Changed" section mentions any test files. A developer executing PRs 1-8 would leave these test files with broken imports, discoverable only when running `pytest`. Falsification test: `TestF6TestFileMigrationInventory` in `tests/test_falsification_extraction_docs_sufficiency.py`. Related: C-10 (no test coverage for viz/mapping/templates). | Developer merges any of PRs 1-8, runs `pytest` — 6+ test files fail with `ModuleNotFoundError` because import paths changed | falsify (2026-05-27, extraction docs sufficiency audit) | Resolved |
| C-122 | 3 | **PR 8 extracts `_PGDataset`-specific functions with base-type hint — LSP gap.** PR 8's metadata extraction table (`extraction_pr_plans.md:922-932`) lists `reconcile_pg_dataset`, `build_pg_metadata_cache`, `detect_country_changes`, `get_country_id`, `get_subset_by_country_id` — all access `_PGDataset`-only attributes (`_country_to_grids_cache` at `handlers.py:1645`, `reconciled_dataframe` at `handlers.py:1646`). The attribute handling spec (line 956) defaults to `_ViewsDataset` type hint. Calling any of these functions with a `_CDataset` instance would `AttributeError` at runtime. The function names make PG-specificity obvious, but the spec's design rule doesn't enforce subclass-specific type hints. Falsification test: `TestF2SubclassTypeHints` in `tests/test_falsification_extraction_docs_solid.py`. Related: C-36 (god class), C-120 (Resolved — attribute spec added, but subclass types not enforced). | Developer implementing PR 8 follows the design rule "pass `_ViewsDataset`" for all extracted functions, including PG-specific ones — type checker doesn't catch wrong-subclass calls, runtime `AttributeError` in production | falsify (2026-05-27, extraction docs SOLID audit) | Resolved — PR 8 table updated to use `_PGDataset`/`_CDataset` type hints for all PG/C-specific functions (2026-05-27) |
| C-123 | 4 | **`ReportingStage` has hardcoded template dispatch — OCP not improved by extraction.** `managers/reporting/stage.py` lines 88-90 and 125-127 use deferred imports for `ForecastReportTemplate` and `EvaluationReportTemplate`. PR 6 (`extraction_pr_plans.md:713-737`) swaps these import paths to `views_reporting` but doesn't change the dispatch pattern. Adding a third report type still requires editing `stage.py`. No registry, protocol, or plugin mechanism exists or is proposed. The existing `ModelPathProtocol` in `types.py` proves the project can use protocols — this pattern should extend to report templates when a third type is needed. Falsification test: `TestF4ReportingStageExtensibility` in `tests/test_falsification_extraction_docs_solid.py`. Related: C-45 (ISP violation in stages), C-48 (concrete dependencies). | A third report type is added to the pipeline (e.g., a summary dashboard, a comparison report) — developer must edit `stage.py` and add another hardcoded deferred import | falsify (2026-05-27, extraction docs SOLID audit) | Resolved — investigation section 9 now documents OCP deferral with `ReportTemplate Protocol` as future improvement trigger (2026-05-27) |
| C-124 | 4 | **19 `isinstance` dispatches on `_PGDataset`/`_CDataset` move to views-reporting unchanged — LSP violations persist.** `mapping.py` has 8 isinstance checks (lines 76, 85, 119, 121, 292, 309, 469, 471), `historical.py` has 2 (lines 305, 313), `forecast.py` has 1 (line 79, also C-114). These dispatch on dataset subclass to select shapefiles, GeoJSON prep, color mapping, hover columns, and entity name resolution. Every isinstance check is a point where adding a new dataset subclass would require editing the dispatching code. The extraction (PRs 3, 4, 6) relocates these files to views-reporting as-is. No polymorphic refactoring, no strategy pattern, no discussion of the issue in any extraction document. The violations are architecturally better-placed in views-reporting (outer layer) than in pipeline-core (inner layer), but they persist as-is. Falsification test: `TestF5IsinstanceDispatchesAddressed` in `tests/test_falsification_extraction_docs_solid.py`. Related: C-114 (forecast.py subset), C-65 (EnsembleManager LSP — different hierarchy). | A new dataset subclass is added (e.g., for a new spatial resolution) — developer must update all 11 isinstance dispatch sites across 3 files in views-reporting | falsify (2026-05-27, extraction docs SOLID audit) | Resolved — investigation section 9 now documents isinstance dispatch deferral with polymorphic refactoring as post-extraction improvement (2026-05-27) |
| C-125 | 3 | **PR 7 recreates heavyweight-dependency tax on ensemble managers (CRP violation).** PR 7 moves `ReconciliationModule` (298 LOC) to views-reporting. Ensemble managers (`ensemble.py:20`, `dataframe_ensemble.py:37`) are the only consumers. After extraction, importing `ReconciliationModule` from views-reporting transitively pulls in the entire views-reporting install (torch ~2 GB, scipy, geopandas, seaborn, plotly, matplotlib — ~2.5 GB total) for 298 LOC of optional reconciliation logic. This is the same heavyweight-dependency problem the extraction solves for pipeline-core, recreated in the other direction. The investigation notes reconciliation is "optional per-ensemble" (line 1508) but does not discuss the disproportionate dependency cost. Mitigation options: (1) keep reconciliation in pipeline-core (simplest — it's consumed only by pipeline-core code), (2) extract reconciliation to a third lightweight package, (3) make views-reporting's heavy deps lazy-loaded so importing reconciliation alone is cheap. Falsification test: `TestF2EnsembleDependencyProportionality` in `tests/test_falsification_extraction_docs_packaging.py`. Related: C-117 (Resolved — install-time cycle), C-47 (deferred imports). | Developer executes PR 7 as written — ensemble managers now import from views-reporting, pulling ~2.5 GB of heavyweight dependencies for 298 LOC of optional reconciliation | falsify (2026-05-27, extraction docs packaging principles audit) | Resolved — PR 7 now includes CRP note with deferred-import mitigation; investigation section 9 documents CRP concern (2026-05-27) |
| C-126 | 4 | **Re-export shims create undocumented runtime import cycle (ADP).** When views-reporting is installed, re-export shims create a bidirectional runtime import path: `views_pipeline_core.modules.statistics.__init__` → `views_reporting.statistics` → `views_pipeline_core.data.handlers`. Documents discuss the install-time cycle (PR 9 lines 1082-1100, "circular pip install") and prevent it via `try/except ImportError`, but never acknowledge the runtime import cycle. Python can resolve this because shims are leaf-level `__init__.py` files (no circular attribute access), but the cycle is fragile — import order matters and developers may encounter confusing `ImportError` or `AttributeError` during development. Falsification test: `TestF3RuntimeImportCycleAcknowledged` in `tests/test_falsification_extraction_docs_packaging.py`. Related: C-47 (deferred imports indicate cycles), C-117 (Resolved — install-time cycle prevented). | Developer working on views-reporting encounters intermittent ImportError when import order between pipeline-core and views-reporting changes (e.g., test discovery order, IDE auto-import) | falsify (2026-05-27, extraction docs packaging principles audit) | Resolved — PR 9 now documents runtime import cycle and resolution; investigation section 9 documents ADP assessment (2026-05-27) |
| C-127 | 4 | **No version coordination strategy between pipeline-core and views-reporting (REP).** Extraction creates two packages with bidirectional dependency: views-reporting depends on pipeline-core (for `_ViewsDataset`, `ModelPathManager`), and pipeline-core's re-export shims depend on views-reporting. Documents cite "independent versioning" as a benefit (investigation line 309) but never specify version pinning, compatibility ranges, or coordinated release strategy. PR 0 line 101 says "Dependency on views-pipeline-core" without a version range. A breaking change to `_ViewsDataset` (rename attribute, change MultiIndex structure, remove method) would break all of views-reporting with no version constraint to prevent incompatible combinations from being installed. Falsification test: `TestF5VersionCoordinationStrategy` in `tests/test_falsification_extraction_docs_packaging.py`. | Developer changes `_ViewsDataset` interface in pipeline-core (e.g., renames `dataframe` attribute, changes `to_tensor()` signature) — views-reporting breaks with no version constraint preventing the incompatible combination | falsify (2026-05-27, extraction docs packaging principles audit) | Resolved — PR 0 pyproject.toml spec updated with version constraint guidance; investigation section 9 documents REP assessment (2026-05-27) |
| C-128 | 4 | **Package-level design principles not discussed in extraction documents.** The three extraction documents (2,080 lines) make implicit decisions about all six package-level principles (REP, CCP, CRP, ADP, SDP, SAP) but never name or acknowledge the framework. The SOLID audit (C-122-124) added class-level principle discussion, but the package-level principles — which are the canonical framework for evaluating package decompositions — are absent. A reviewer cannot verify whether the decomposition was evaluated against these criteria. Falsification test: `TestF7PackagePrinciplesAcknowledged` in `tests/test_falsification_extraction_docs_packaging.py`. Related: C-122-124 (SOLID principles — class-level, Resolved). | Reviewer evaluates the extraction plan and asks "which package design principles justify this decomposition?" — no answer in the documents | falsify (2026-05-27, extraction docs packaging principles audit) | Resolved — investigation section 9 now includes Package Principle Assessment covering all 6 principles (2026-05-27) |
| C-129 | 4 | **Multi-class files relocated or retained without structural discussion.** Three files contain multiple classes but the extraction documents never acknowledge or justify the multi-class pattern: (1) `statistics.py` (769 LOC) contains `PosteriorDistributionAnalyzer` and `ForecastReconciler` — distinct responsibilities with distinct consumers (`handlers.py:5` vs `reconciliation.py:10`). PR 2 copies this as "Exact copy" to `views_reporting/statistics/statistics.py`. (2) `handlers.py` retains 7 classes post-extraction (`_ViewsDataset`, `_PGDataset`, `PGMDataset`, `PGYDataset`, `_CDataset`, `CMDataset`, `CYDataset`, ~953 LOC) — a full inheritance tree in one file. The investigation's "Residual `_ViewsDataset`" section (lines 149-163) describes LOC reduction but never mentions the 7-class structure. (3) `aggregator.py` has 2 classes (`_ModelSpec` dataclass + `AggregationModule`) — tightly coupled, justified exception. The documents should state which multi-class files are intentional exceptions (e.g., inheritance hierarchies, tightly coupled dataclass+class) and which should be split post-extraction. Falsification tests: `TestF1StatisticsFileSplitting`, `TestF2HandlersFileStructure`, `TestF5MultiClassFileAwareness` in `tests/test_falsification_extraction_docs_fileorg.py`. Related: C-36 (god class). | Developer implementing PR 2 copies statistics.py as-is to views-reporting, inheriting 2-class structure; post-extraction, a developer modifying ForecastReconciler must navigate PosteriorDistributionAnalyzer's 500 LOC to find it | falsify (2026-05-27, extraction docs file organization audit) | Resolved — investigation File Organization Assessment now documents each multi-class file with rationale: statistics.py deferred split, handlers.py intentional hierarchy, aggregator.py tightly coupled (2026-05-27) |
| C-130 | 4 | **File organization principles absent from extraction documents.** The extraction documents discuss 11 design principles (5 SOLID + 6 package-level) but never articulate file organization goals: one class/concept per file, no dumping grounds, folder names convey responsibility, navigability for new developers. This is the governance layer between class design and package design — where a new developer's first impression of the codebase forms. Without stated goals, the "Exact copy" pattern transfers file-level structural debt to views-reporting without the team consciously choosing to defer file-level improvements. Falsification test: `TestF6FileOrganizationPrinciples` in `tests/test_falsification_extraction_docs_fileorg.py`. Related: C-128 (Resolved — package principles added), C-129 (multi-class files). | New developer onboards to views-reporting, sees statistics.py with 2 unrelated classes and handlers.py with 7 classes — cannot determine whether this is intentional design or debt | falsify (2026-05-27, extraction docs file organization audit) | Resolved — investigation File Organization Assessment states goals: one main concept per file, utility files acceptable when cohesive, "Exact copy" is conscious deferral not endorsement (2026-05-27) |
| C-131 | 4 | **ADR-054 and investigation claim "~2.5 GB dependency reduction" but torch (~2 GB) is marked "Shared/REVIEW".** ADR-054 Positive Consequences (line 116) states "~2.5 GB dependency footprint reduction from pipeline-core (torch, scipy, geopandas, plotly, matplotlib, seaborn, markdown, joblib leave)." The investigation dependency table (line 35) marks torch as "Shared / REVIEW" because `handlers.py:15` uses it for `_ViewsDataset.to_tensor()` — a core data container method that stays in pipeline-core. If torch cannot leave, the actual reduction is ~510 MB (scipy ~150 MB, geopandas ~200 MB, plotly ~50 MB, matplotlib ~80 MB, seaborn ~20 MB, markdown ~5 MB, joblib ~5 MB). The ~2.5 GB figure is repeated in the investigation executive summary and ADR-054. Related: C-127 (Resolved — version coordination). | Developer executes PR 9 dependency cleanup, removes torch from pipeline-core's pyproject.toml based on the "~2.5 GB reduction" claim — `_ViewsDataset.to_tensor()` fails with `ModuleNotFoundError: No module named 'torch'` at runtime | review-diff (2026-05-27) | Resolved — ADR-054 and investigation updated to distinguish certain removals (~510 MB) from conditional (torch ~2 GB, pending PR 8 outcome) (2026-05-27) |
| C-132 | 2 | **ADR-054 shim pattern catches bare `except ImportError` — masks transitive dependency failures with misleading diagnosis.** The prescribed `try/except ImportError` re-export shim (ADR-054 lines 67-74) catches `ImportError` without chaining via `from e`. When `views_reporting` is installed but a transitive dependency (torch ~2 GB C extensions, scipy, geopandas) has a broken installation, ABI mismatch, or CUDA version conflict, the shim catches the *wrong* `ImportError` and tells the user "Install views-reporting" — a completely misleading diagnosis. The original traceback is silently discarded (no `from e`). Demonstrated with simulated broken torch: ADR-054 pattern produces `Original cause preserved? None`. The codebase's own precedent at `dataloaders.py:1171` already uses the correct `except ImportError as e: raise ... from e` pattern. All 9 shim examples (1 in ADR-054, 7 in extraction_pr_plans.md, 1 in investigation report) omit `from e`. Every extraction PR (1-8) will introduce shims with this defect unless the pattern is corrected before implementation begins. Falsification tests: `TestF1_NestedImportErrorMasking` (2 tests), `TestF3_MissingFromE` (1 test) in `tests/test_falsification_try_except_critical_infra.py`. Related: C-71 (Resolved — broad `except Exception` in `_load_c_dataset`, same catch-too-broadly class), C-126 (runtime import cycle), C-117 (Resolved — circular dep). | Developer implements PR 1-8 shims using the prescribed bare `except ImportError` pattern; views-reporting is installed but torch has an ABI mismatch after a conda update — every shim reports "Install views-reporting" instead of the actual torch error, and the original traceback is lost | falsify (2026-05-28, try/except critical infrastructure audit) | Resolved — all 9 shim patterns updated to `except ImportError as e: raise ... from e` matching dataloaders.py precedent (2026-05-28) |
| C-136 | 4 | **`modules/transformations/README.md` orphaned after extraction.** **Resolved (2026-05-29):** README was deleted during PR 1 extraction. Confirmed absent via orphaned file scan during PR 10. | N/A | falsify (2026-05-28, PR 1 extraction audit) + ADR-054 (2026-05-29) | Resolved |
| C-137 | 3 | **Test imports of removed packages not guarded with `pytest.importorskip`.** 5 test files imported from packages removed during ADR-054 extraction without skip guards — CI failures instead of graceful skips. **Fixed (2026-06-01):** PR #109 added `pytest.importorskip` guards to all 5 affected files. Related: C-142 (broader guard coverage). | N/A | PR #103 review (2026-06-01) | Resolved |
| C-138 | 3 | **No behavioral equivalence tests between extracted shims and original modules.** Re-export shims could silently return different objects than the originals. **Fixed (2026-06-01):** PR #121 added golden-output behavioral equivalence tests for all extracted modules. | N/A | PR #103 review (2026-06-01) | Resolved |
| C-139 | 3 | **No minimal CI workflow to exercise `importorskip` guards in views-reporting-absent environment.** Guards added (C-137) but never tested without views-reporting installed. **Fixed (2026-06-01):** PR #117 added `run_pytest_minimal.yml` CI workflow verifying guards trigger skips not failures. | N/A | PR #103 review (2026-06-01) | Resolved |
| C-142 | 3 | **Test files import from views-reporting without `pytest.importorskip` guard — CI fails instead of skipping.** Multiple test files had module-level imports from extracted shim modules or views-reporting directly. In CI (where views-reporting is not installed), these tests fail with `ModuleNotFoundError` instead of being skipped. **Resolved (2026-06-01):** PRs #129, #132, #133 added `pytest.importorskip("views_reporting")` guards to all 9+ affected test files. CI now skips these tests gracefully. Related: C-137 (import guards — resolved for 5 files), C-139 (minimal CI). | N/A | PR #103 review (2026-06-01) | Resolved |
| C-149 | 4 | **`ModelManager` loaded `config_modelset.py` for single models — fixed.** Originally `model.py:173` attempted to load `config_modelset.py` for every model including single models. **Fixed (2026-06-04):** `config_modelset` loading removed from `ModelManager`; now only in the three ensemble managers where it belongs. | N/A — fixed | review (feature/config-modelset, 2026-06-04) | Resolved |
| C-150 | 2 | **`_build_absolute_directory` made `config_modelset.py` mandatory despite CIC "optional" claim — fixed.** `_initialize_ensemble_specific_scripts()` originally added `configs/config_modelset.py` via `_build_absolute_directory()`, which raised `FileNotFoundError` when `validate=True` and the file was missing. **Fixed (2026-06-05):** replaced with direct `Path.exists()` check — file is only added to `self.scripts` when present on disk. `_load_config` returns `None` for absent scripts, and all three ensemble managers handle `None` gracefully. Regression tests in `test_falsification_config_modelset_done.py` and `test_ensemble_path.py`. Related: C-49 (original fail-loud fix — preserved for mandatory files), C-148 (config_modelset merge). | N/A — fixed | falsify (2026-06-05, config_modelset "done" audit) | Resolved |
| C-151 | 3 | **config_modelset collision warning and merge precedence untested — RESOLVED.** All three ensemble CICs (§4) guaranteed collision warning and merge precedence but no tests enforced them. **Fixed (2026-06-05):** Added `TestConfigModelsetMerge` class (5 tests) to each ensemble manager test file — 15 tests total covering: no-modelset passthrough, disjoint key merge, overlap precedence, collision warning via `caplog`, and in-place/copy mutation semantics. Related: C-148 (collision behavior). | N/A — fixed | test-review (2026-06-05, config_modelset splash zone) | Resolved |
| C-153 | 3 | **ModelPathManager CIC critically stale — RESOLVED.** §12 documented a fixed deviation (C-49), §5 omitted optional script handling (C-150), §10 missed test files. **Fixed (2026-06-05):** §12 false deviation removed, §5 documents mandatory vs optional script distinction, §10 adds `test_ensemble_path.py` and `test_falsification_config_modelset_done.py`. Last reviewed date updated. Related: C-49, C-150, C-86. | N/A — fixed | review-base-docs (2026-06-05, config_modelset splash zone) | Resolved |
| C-154 | 3 | **ADR-011 does not cover config_modelset.py — RESOLVED.** ADR-011 listed 5 config types; config_modelset.py was absent. **Fixed (2026-06-05):** Added config_modelset.py as 6th config type (Operational, Optional) with merge-precedence semantics and template reference. Added "Mandatory vs Optional" subsection to Rationale. ADR README updated with ADRs 051-054. Related: C-148, C-150, C-151. | N/A — fixed | review-base-docs (2026-06-05, config_modelset splash zone) | Resolved |
| C-155 | 4 | **CIC §10 test counts re-drifted — RESOLVED.** EnsembleManager.md claimed 60+ methods (actual 51), DataFrameEnsembleManager.md claimed 36/11 (actual 45/13), PredictionFrameEnsembleManager.md was accurate pre-fix but updated to 48/9. **Fixed (2026-06-05):** All three CICs updated with accurate post-C-151 test counts and `TestConfigModelsetMerge` class documented. Related: C-86 (same pattern). | N/A — fixed | review-base-docs (2026-06-05, config_modelset splash zone) | Resolved |

### Mitigated Concerns

| ID | Original Tier | Description | Resolution | Date |
|----|--------------|-------------|------------|------|
| C-143 | 4 | **Ensemble managers import `ReconciliationModule` directly from `views_reporting` inside method bodies — late crash without views-reporting.** `ensemble.py:_reconcile_pg_with_c()` and `dataframe_ensemble.py:_reconcile_pg_with_c()` use deferred imports. If views-reporting is not installed and reconciliation is configured, the crash happens after sub-model forecasting completes. **Mitigated (2026-06-01):** PR #129 added `importlib.util.find_spec("views_reporting")` pre-flight check in `CoreConfigSniffer._check_reconciliation_config()` — import availability validated at config audit time before any compute. Related: C-125 (CRP concern — resolved), C-78 (pre-flight validation). | Pre-flight check catches missing views-reporting at config audit time. Residual: deferred import pattern still exists but crash window eliminated. | 2026-06-01 |
| C-110 | 4 | **12 PF models in views-models silently affected by mandatory `skip_predictions_delivery` key.** 12 baseline PF models omit the key; 7 HydraNet models had it set to `False`. All 19 must add/update the key per the companion spec. | views-models PR spec written (`reports/views_models_mandatory_skip_predictions_delivery_spec.md`). ADR-053 documents the decision. Execution pending — Resolved when views-models companion PR merges. | 2026-05-27 |
| C-111 | 3 | **No test for ensemble with explicit PF format + missing `skip_predictions_delivery`.** CIC §3 guarantee untested. | Two tests added: `test_ensemble_explicit_pf_missing_skip_delivery_raises` (KeyError) and `test_ensemble_explicit_pf_with_skip_delivery_passes` (happy path). Also added integer truthy, None, fail-fast ordering, sweep characterization, and save arg assertion tests. CIC §10 updated. | 2026-05-26 |
| C-02 | 1 → 4 | **Target name fragility.** Originally reported as hard crash on non-standard target names. Code has evolved to accept arbitrary target names: `evaluation/stage.py:93` uses `target_identifier = target` directly. Test `test_B1_non_standard_naming_allowed` proves it. | Dead `_get_eval_file_paths()` method removed; `generate_evaluation_report_name()` param renamed from `conflict_type` to `target_identifier`; misleading docstring fixed. Tech debt cleanup 2026-04-02. | 2026-04-02 |
| C-49 | 2 | **`ModelPathManager._build_absolute_directory` returned `None` on missing dirs with `validate=True`.** Logged a warning and silently assigned `None` to path attributes (`self.data_raw`, `self.reports`, ...), causing cryptic `TypeError: unsupported operand type(s) for /: 'NoneType' and 'str'` far from the root cause when downstream baseline/stepshifter managers did path arithmetic. Direct violation of the "Fail Loud and Proud" principle. | Option B (Diagnostic) applied: `_build_absolute_directory` now raises `FileNotFoundError` with a descriptive message when a path is missing and `validate=True`, matching the existing `_get_model_dir` pattern. Two permissive tests in `test_model_path.py` replaced with fail-loud assertions; `test_ensemble_path.py` fixture expanded to build a complete ensemble skeleton. 1092 tests green. | 2026-04-11 |
| C-46 | 2 | **No architectural boundary enforcement in CI.** Dependency direction (inner layers must not import outer layers) maintained by developer discipline only. Investigation (2026-04-11) showed the register description was partially stale: `tests/test_boundary_enforcement.py` already existed and enforced `data/`/`configs/`/`modules/` boundaries via AST parsing, but used file-level exemptions that hid real violations and did not scan `files/` at all. | Enforcement strengthened: boundary test now also scans `files/` (Layer 2) via new `TestUtilityLayerBoundaries` class; file-level exemptions for `modules/appwrite/file.py` and `modules/logging/logging.py` removed (both fixed by C-43 in same PR); remaining exemption narrowed from file-level to *granular symbol-level* — only `ModelManager`, `EnsembleManager`, `EnsemblePathManager` from `check.py` are allowed, and any additional manager import triggers a test failure. Runs in standard pytest suite via `run_pytest.yml` on every PR. Residual smell tracked as C-50. ADR-002 Enforced Boundaries section added. | 2026-04-11 |
| C-52 | 2 | **Drift detection silently lost during viewser→datafactory migration.** Drift detection is a viewser-specific feature (`Queryset.publish().fetch_with_drift_detection()`). Datafactory models have no equivalent. | Accepted: `_fetch_data_from_datafactory()` returns `None` for alerts. ViewsDataLoader CIC §11 documents the gap. Source-agnostic drift detection interface deferred until views-datafactory supports equivalent functionality. | 2026-04-22 |
| C-69 | 1 | **`wandb.AlertLevel.WARNING` does not exist — runtime `AttributeError`.** `ensemble.py:634` used `wandb.AlertLevel.WARNING`. The wandb enum only has `INFO`, `WARN`, `ERROR`. This crashed at runtime when reconciliation returned `None`. Masked in tests by mocking wandb. | Fixed: `WARNING` → `WARN` in both `ensemble.py` and `dataframe_ensemble.py`. Test updated to not mock a non-existent enum member. | 2026-05-18 |
| C-70 | 1 | **`_resolve_evaluation_sequence_number("complete")` returned `None` — `TypeError` on `range(None)`.** `model.py:870` returned `None` for `eval_type="complete"`. All callers pass the result to `range()`, causing `TypeError: 'NoneType' cannot be interpreted as an integer`. Anyone running `--eval_type complete` got an immediate crash. Masked in tests by asserting `None` return. | Fixed: now raises `NotImplementedError` with a clear message. Test updated to expect `NotImplementedError`. | 2026-05-18 |
| C-73 | 2 | **`dataset_class()` returned `None` for unknown LOA — deferred crash.** `model.py:823-828` returned `None` when `loa` was not `"cm"` or `"pgm"`. The `None` propagated until it crashed with a cryptic error at a later call site. | Fixed: now raises `ValueError` with the invalid LOA and valid options listed. | 2026-05-18 |

### Closed Concerns

| ID | Description | Resolution | Date |
|----|-------------|------------|------|
| C-04 | **Silent WandB failure.** Asymmetric error handling — some calls suppress, some propagate, some have no try/except. | All `wandb.log()` calls wrapped with `_safe_wandb_log()`. Consistent suppress-and-log pattern. | 2026-04-04 |
| C-09 | **ensemble_aggregator vs dataloaders duplication.** Investigation found these serve different purposes: `AggregationManager` (ensemble pooling) vs `ViewsDataLoader` (data fetching). Not duplicated. | False alarm — different modules with different responsibilities | 2026-04-02 |
| C-12 | **Deprecated shims in cli/utils.py.** `parse_args()` and `validate_arguments()` deprecated wrappers. | File removed — no imports existed anywhere in codebase or tests | 2026-04-02 |
| C-13 | **audit_suite.py tests non-existent method.** G1/G2/R1 called `_get_conflict_type()`. | Tests rewritten to validate current opaque-identifier behavior. Report updated. | 2026-04-03 |
| C-19 | **ForecastingModelManager failure modes untested.** 5 of 10 CIC failure modes had zero test coverage. | 5 tests added: Training, Evaluation, Forecasting, DataFetch exception propagation + wrong sequence count. | 2026-04-04 |
| C-20 | **No pipeline ordering safety.** No tests verified execution order. | 3 tests added: data-fetch-before-tasks, train→evaluate→forecast order, missing-data-loader failure. | 2026-04-04 |
| C-22 | **ADR-008 systematic violation.** ~10 PipelineException raises without preceding logger.error. | Added `logger.error()` before all raise sites across 4 files. | 2026-04-03 |
| C-23 | **NaN silently propagates into training tensors.** `_ViewsDataset.to_tensor()` had zero NaN checks. | Added `_check_tensor_nan()` — raises `ValueError` on NaN. Falsification test passes. | 2026-04-04 |
| C-24 | **ConfigurationManager silently overrides safety-critical parameters.** `dict.update()` merge with no conflict detection. | Added `_SAFETY_CRITICAL_KEYS` + conflict logging in `_get_raw_combined_config()`. Falsification test passes. | 2026-04-04 |
| C-25 | **ReconciliationModule.max_workers inoperative.** `ProcessPoolExecutor(max_workers=None)` ignored computed value. | Fixed to `ProcessPoolExecutor(max_workers=num_of_workers)`. | 2026-04-03 |
| C-27 | **Version constraint for views-evaluation stale.** `pyproject.toml` declared `"^0.3.0"`, actual installed v0.5.0. | Updated to `"^0.5.0"`. | 2026-04-04 |
| C-28 | **Stale module mocks reference deleted infrastructure.** `audit_suite.py` and `test_falsification_biggest_risks_found.py` mocked deleted `evaluation_manager`. | Removed stale mocks. | 2026-04-04 |
| C-32 | **`calculate_mean_evaluation_metrics()` uses first item's keys only.** Metrics absent from first group silently dropped from mean. | Fixed to collect union of all keys. Test now passes. | 2026-04-04 |
| C-33 | **ADR file-to-header number mismatch.** 4 ADR files (039-042) had internal header numbers (050, 051, 057, 058) different from filenames. 2 phantom ADRs (030, 033) referenced in CICs/code but never existed. 97 cross-references across 44 files. | Headers renumbered to match filenames; all cross-references updated. | 2026-04-04 |
| C-14 | **Cache filename collision across sources.** Cache filenames used `{partition}_viewser_df.parquet` regardless of actual data source. | Cache filenames now encode the source: `{partition}_{source}_df{ext}`. Source switch cannot load stale data from the other source. PRs 1-4 of viewser→datafactory migration. | 2026-04-22 |
| C-51 | **`get_data()` hardcodes viewser as sole data source.** Dict descriptors crashed with `AttributeError` on `.publish()`. | `_detect_data_source()` + `_fetch_data()` dispatch. `get_data()` routes to `_fetch_data_from_viewser()` or `_fetch_data_from_datafactory()` based on queryset type. 8 dispatch tests + 24 characterization tests. PRs 1-4. | 2026-04-22 |
| C-53 | **`use_saved` overloaded as cache control + source selector.** After Phase 1 workaround, `use_saved=True` implicitly meant "not a viewser model." | Source selection is now automatic via `_detect_data_source()`. `use_saved` controls only caching. PRs 1-4. | 2026-04-22 |
| C-41 | **Missing ADRs for 6 external integration decisions.** No ADR documented: Appwrite integration, three-destination persistence, graceful degradation, PredictionSaver protocol, rolling-origin evaluation, WandB integration. | All 6 covered: ADR-046 (Appwrite), ADR-047 (persistence), ADR-048 (savers), ADR-049 (rolling-origin), ADR-050 (WandB). Graceful degradation in ADR-046 §1 and ADR-047. | 2026-04-08 |
| C-43 | **DIP violation: `modules/` imports from `managers/`.** Original entry cited `modules/dataloaders/dataloaders.py:12` (already fixed at time of review). Investigation (2026-04-11) surfaced 4 remaining legacy `ModelPathManager` imports hidden behind file-level exemptions in the boundary test: `files/utils.py:130`, `modules/appwrite/file.py:70`, `modules/logging/logging.py:7`, `modules/validation/ensemble/check.py:178` + `:238`. | All 4 sites migrated to canonical `views_pipeline_core.data.model_path` (Layer 1). `check.py:238` split so `ModelPathManager` comes from `data/`; remaining `ModelManager`/`EnsembleManager`/`EnsemblePathManager` imports preserved under a narrowed granular exemption and tracked as C-50. Six `@patch` targets in `tests/test_modules/test_ensemble_check.py` updated to the canonical path. Boundary-test exemptions for `appwrite/file.py` and `logging/logging.py` removed. 1093 tests green. | 2026-04-11 |
| C-109 | **No ADR documents the eval-path Track B retirement decision.** Decision rationale existed only in `reports/track_b_retirement_views_pipeline_core.md`, not in `documentation/ADRs/`. | ADR-053 documents the eval-path Track B retirement decision, distinguishes it from the forecasting-path Track B (ADR-048), and cross-references the views-models companion spec. ADR-048 updated with cross-reference. | 2026-05-27 |
| C-97 | **DF ensemble timestamp divergence masked by fuzzy file matching.** All 8 timestamp derivation sites used `get_latest_model_artifact_path()` independently. | All 8 sites migrated to `resolve_artifact_path()`. Producer and consumer use the same method. Fuzzy matching no longer masks a divergence. | 2026-05-27 |
| C-99 | **PF save path ignores `--artifact_name` — uses latest artifact timestamp.** `model.py` eval and forecast paths always derived timestamp from latest artifact. | `resolve_artifact_path(run_type, artifact_name)` added to `ModelPathManager`. Save-path derivation respects `--artifact_name`. xfail test passes. ModelPathManager CIC updated. | 2026-05-27 |

---

## Stories (Domain Layer Extraction)

Ordered sequence of incremental PRs moving architecture from "PIPELINE" toward "CONFLICT FORECASTING SYSTEM." S-01 through S-04 are additive-only (no existing code modified). S-05 through S-07 wire domain types into existing code. S-08 is governance.

| ID | Title | Scope | Dependencies | Related Concerns |
|----|-------|-------|-------------|-----------------|
| S-01 | Introduce `SpatialLevel` value object | Small | None | C-38 |
| S-02 | Introduce `TemporalPartition` / `PartitionSet` | Small | None | C-17, C-38 |
| S-03 | Introduce `ForecastHorizon` value object | Small | None | C-38, C-39 |
| S-04 | Introduce `ReconciliationInvariants` | Small | None | C-38 |
| S-05 | Wire `SpatialLevel` into validation sniffers | Medium | S-01 | C-38 |
| S-06 | Wire `PartitionSet` + `ForecastHorizon` into config sniffer | Medium | S-02, S-03 | C-17, C-38 |
| S-07 | Consolidate step-mapping via `ForecastHorizon` | Large | S-03 | C-26, C-29, C-39 |
| S-08 | ADR-046 (Domain Layer) + CI import boundary test | Medium | S-01..S-04 | C-38 |

### Story Details

**S-01: SpatialLevel** — Create `domain/spatial.py`. Frozen enum with `CM` and `PGM` members carrying canonical index names (`("country_id", "month_id")` / `("priogrid_gid", "month_id")`). `from_str()` parser replaces bare string comparisons. Zero imports from outside `domain/`.

**S-02: TemporalPartition / PartitionSet** — Create `domain/temporal.py`. `TemporalPartition(start, end)` with `base_origin` property (`start - 1`). `PartitionSet(train, test)` validates non-overlap at construction (`train.end < test.start`). `validate_rolling_origin(time_steps, max_shift_count)` replaces inline arithmetic.

**S-03: ForecastHorizon** — Create `domain/horizon.py`. `ForecastHorizon(time_steps, stride, max_shift_count)`. Properties: `n_sequences` (`max_shift_count + 1`), `required_test_length` (`time_steps + max_shift_count`). Method: `build_step_mappings(base_origin, n_sequences)`. `default()` returns production values `(36, 1, 12)`.

**S-04: ReconciliationInvariants** — Create `domain/reconciliation.py`. Named mathematical constraints: `sum_tolerance=1e-2`, `zero_atol=1e-8`, `enforce_non_negativity=True`, `preserve_zeros=True`. `check_sum_constraint()`, `check_zero_preservation()`, `validate_all()` returns violation list.

**S-05: Wire SpatialLevel** — Replace `SUPPORTED_LEVELS = {"cm", "pgm"}` in `core_config_sniffer.py` and `EXPECTED_INDEX_NAMES` in `core_data_sniffer.py` with derivations from `SpatialLevel` enum.

**S-06: Wire Partition + Horizon** — Replace inline arithmetic in `core_config_sniffer._check_evaluation_contract()` with `PartitionSet`/`ForecastHorizon` construction and validation.

**S-07: Consolidate Step-Mapping** — Extract `resolve_base_origin()` to `domain/origin.py`. Replace duplicated step-mapping in `model.py:1715` and `stage.py:251` with `ForecastHorizon.build_step_mappings()`.

**S-08: Governance** — Write ADR-046 (Domain Layer). Add CI test that `domain/` imports only stdlib + numpy. Cross-reference stories from risk register entries.

### DoD per story type

**Additive stories (S-01..S-04):** New file in `domain/`, test file in `tests/test_domain/`, zero imports from outside `domain/`, no existing code modified, all tests pass.

**Wiring stories (S-05..S-07):** Existing code imports domain types, inline logic replaced with domain method calls, all existing tests pass without modification, no new behavior.

**Governance (S-08):** ADR exists, import boundary test passes, risk register cross-references complete.

---

## Process

Concerns are opened during:
- Expert reviews
- Tech debt audits
- Falsification audits
- Post-migration audits
- Repo assimilation

Concerns are closed when:
- The underlying issue is resolved (code change merged)
- The risk is formally accepted with documented rationale
- The concern is superseded by a different approach
